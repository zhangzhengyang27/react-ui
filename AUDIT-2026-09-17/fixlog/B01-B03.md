# B01(core)/ B03(Combobox 家族)修复日志(含 B02-8 跨批协同)

修复人:修复工程师(B01 + B03 批次)。日期:2026-09-17。
仓库:/Users/zhangzhengyang/Desktop/PROJECT/react-ui(工作树,未 commit/push)。

## 测试总况

- 命令:`cd packages/ui && pnpm test`(vitest run,node v22.12.0)
- 全量结果:**3894 passed / 3 failed**(325 个测试文件,322 通过)
- 3 个失败均在**并行 agent 负责的目录**:`Menu/Menu.test.tsx`(子菜单展开)、`PinInput/PinInput.test.tsx`(受控回显)、`Rating/Rating.test.tsx`(键盘 change)——git status 显示这三个组件源码/测试与 `packages/hooks/src/use-move/use-move.ts` 均为并行 agent 的进行中改动,与本批修改文件无 import 依赖;隔离复跑时其中一个已随对方继续编辑而通过(波动佐证属对方中间态)。本批负责目录(core/** 与 Combobox 家族)相关测试**全部通过**。
- 本批改动文件 `tsc --noEmit` 过滤检查:0 错误(仓库其余目录存在大量存量 TS 错误,非本批引入)。

## 修复明细

| 单号 | 状态 | 改动文件与要点 | 测试结果 |
| --- | --- | --- | --- |
| B01-1 | 已修 | `core/UIProvider/UIProvider.tsx`、`core/UIProvider/UI.context.ts`(类型导入):UIProviderProps 新增 `stylesTransform?: UIStylesTransform` 与 `getStyleNonce?: () => string \| undefined`,解构进 context value(`getStyleNonce: getStyleNonce ?? (() => undefined)`、`stylesTransform`),并加入 useMemo 依赖。Box `sx` 与 emotion `emotionTransform` 集成由死参数变为可用,CSP nonce 注入链路打通 | 全量套件通过(无既有测试编码该 context 值) |
| B01-2 | 已修 | `core/UIProvider/UIProvider.tsx`:新增 `isRootProvider = parentContext === null`,color-scheme effect 首行 `if (!isRootProvider ...) return` 且依赖加 `isRootProvider`——内层实例跳过写 `data-ui-color-scheme` 属性与 matchMedia 订阅,只有根 Provider 写属性,嵌套首挂被覆盖的确定性错误终态消除(嵌套未受控本就继承外层方案,内层写属性是纯冗余) | 通过(全库无嵌套 colorScheme 行为测试) |
| B01-3 | 已修 | `core/UIProvider/UIProvider.tsx`:`clearColorScheme` 改为 `setInternalColorScheme(parentContext?.colorScheme ?? 'light')`,`parentContext?.colorScheme` 纳入 useCallback 依赖 | 通过 |
| B01-4 | 已修 | `core/UIProvider/UIProvider.tsx`(新增 `localStorageKey` prop,默认 `'ui-color-scheme-value'`,进 context value 与 memo 依赖)、`core/UIProvider/UI.context.ts`(UIContextValue 增 `localStorageKey?`)、`core/UIProvider/use-ui-color-scheme/use-ui-color-scheme.ts`:`setColorScheme` 内 `localStorage.setItem`、`clearColorScheme` 内 `removeItem`(均 try/catch,SSR/隐私模式静默跳过)——ColorSchemeScript 启动脚本读取的键从此有写入方,用户上次选择刷新后可回显;键名与 ColorSchemeScript 的 localStorageKey prop 对齐可配 | core(含 __audit-verify__)+ 唯一消费方 CodeHighlight(45 用例)通过 |
| B01-5 | 已修 | `core/ThemeProvider/global.css`:以 default-css-variables-resolver/get-css-color-variables 运行时输出为准修正三处回退块——①light 侧(含 :root 回退)`-light/-light-hover` 由 `rgba(X-6,.1/.12)` 改 `var(X-1)/var(X-2)`、`-light-color` 由 `var(X-6)` 改 `var(X-9)`;②dark 侧 `-light/-light-hover` 改为运行时 `darken(color[9],0.5/0.3)` 的实际输出值(如 blue `rgba(12,50,86,1)/rgba(17,70,120,1)`,全部 14 色按 DEFAULT_COLORS 计算)、`-light-color` 由 `var(X-3)` 改 `var(X-0)`;③`--ui-color-default-color` light/:root 由 `gray-9` 改 `black`(dark 侧本就 white 一致);④三个块(:root 回退、dark、light)补齐 `--ui-color-success`(light/dark 均 teal-8)与 `--ui-color-disabled/-color/-border`(light:gray-2/5/3,dark:dark-6/3/4) | 通过(CSS 不参与 jsdom 断言,值为脚本从 DEFAULT_COLORS+darken 公式计算) |
| B01-6 | 已修 | `core/factory/polymorphic-factory.tsx`:`withProps` 产物补 `Extended.classes = Component.classes`、`Extended.varsResolver = Component.varsResolver`,与非 polymorphic factory 对齐,`Paper.withProps(...).classes` 不再 undefined | Paper/Kbd 测试通过 |
| B01-7 | 已修 | `core/UIProvider/merge-ui-theme/merge-ui-theme.ts`:`isValidPrimaryShade` 开头加 `if (typeof shade !== 'number' \|\| Number.isNaN(shade)) return false`,`primaryShade: { dark: undefined }` 类显式 undefined 现在抛既定 INVALID_PRIMARY_SHADE_ERROR 而非裸 TypeError | 通过 |
| B02-8 | 已修 | `components/Combobox/ComboboxDropdown.tsx`(打开期间恒挂 `data-ui-stop-propagation="true"`,对齐 PopoverDropdown)、`components/Combobox/ComboboxTarget.tsx` 与 `components/ComboboxPopover/ComboboxPopoverTarget.tsx`(`'data-ui-stop-propagation': ctx.opened ? 'true' : undefined`,对齐 PopoverTarget)。Modal 内打开 Select/Autocomplete/ComboboxPopover 等按 Escape 只关下拉,不再双关 Modal;ComboboxPopover 的 DropdownSearchInput 经 Combobox.Dropdown 元素级标记一并覆盖 | Select(30)/Autocomplete(6)/ComboboxPopover 相关用例通过 |
| B03-1 | 已修+新增测试 | `components/Combobox/ComboboxTarget.tsx`:onClick 增加事件命中判定 `isEditableTextInputTarget(event.target)`(INPUT[非按钮类 type]/TEXTAREA 且非 readOnly)——wrapper 内层可编辑输入框点击不参与 toggle,对齐 Select 的 ignoreClick 语义(readOnly 输入仍 toggle);`components/MultiSelect/MultiSelect.tsx`:InputBase onMouseDown 在 `searchable && !disabled && !opened` 时 `handleOpenedChange(true)`(对齐 Select.tsx:395-403 的 mousedown 打开)。新增测试「searchable 时点击输入框本体打开下拉、再次点击不关闭」 | MultiSelect.test.tsx 7/7 通过 |
| B03-2 | 已修+新增测试 | 复用 B03-1 的 ComboboxTarget 命中判定;`components/TagsInput/TagsInput.tsx`:TagsInputTarget 的 onClick 在非按钮命中且 `!disabled && !isMaxTags && !opened` 时兜底 `setOpened(true)`——保持既有"点击打开建议下拉"语义(老测试 click 打开不受影响),展开态点击输入框只聚焦不收起。新增测试「点击输入框本体不关闭已展开的建议下拉」 | TagsInput.test.tsx 8/8 通过 |
| B03-3 | 已修+新增测试 | `components/Cascader/Cascader.tsx`:新增 `focusFirstPanelItem()`(首列首项,搜索模式回落搜索列表首项,新增 `searchListRef`),单选 InputBase 与多选 PillsInput.Field 的 onKeyDown 在 `_opened && ArrowDown` 时移焦——键盘可从输入框进入面板。新增测试:「打开态在输入框按 ArrowDown 聚焦首列首项(含列内继续移动)」「搜索模式下 ArrowDown 聚焦搜索结果首项」 | Cascader.test.tsx 11/11 通过 |
| B03-4 | 已修 | `components/Combobox/Combobox.tsx`:打开/选项集变化 effect 中,若 `selectedValues[0]` 仍在注册表则 activeIndex 定位到该项(否则回落首个可用项),并对该选项 DOM `scrollIntoView?.({ block: 'nearest' })`(jsdom 无实现,可选调用兜底)。Select/MultiSelect/Autocomplete/TreeSelect 均经 Combobox 统一生效,无需逐个接线;aria-activedescendant 打开时指向当前值 | Select(30)/MultiSelect(7)/Autocomplete(6) 通过,含"键盘重开清残留搜索"等打开态用例 |
| B03-5 | 已修 | `components/TagsInput/TagsInput.tsx`:粘贴切分正则转义集合加 `-`(`/[.*+?^${}()\|[\]\\-]/g`),`splitChars={[',','-',;]}` 不再把 `,`-`;` 区间字符(含数字)当分隔符 | TagsInput.test.tsx 通过 |
| B03-6 | 已修 | `components/TagsInput/TagsInput.tsx`:clear-all 与胶囊移除两处 CloseButton 补 `onMouseDown={e => e.preventDefault()}`,`handleClear/handleRemove` 末尾 `inputRef.current?.focus()`——按钮随值变化卸载后焦点不再丢 body | TagsInput.test.tsx 通过 |
| B03-7 | 已修 | `components/MultiSelect/MultiSelect.tsx`:胶囊移除 CloseButton 补 onMouseDown preventDefault,`handleRemove/handleClear` 末尾回焦输入框(renderPill 的 onRemove 路径同样受益) | MultiSelect.test.tsx 通过 |
| B03-8 | 已修 | `components/MultiSelect/MultiSelect.tsx`:renderPill 分支的包装 `<span key>` 展开 `{...pillsReorder.getPillProps(index)}`(draggable/data-ui-pill-index/键盘处理等),withPillsReorder + renderPill 组合不再静默失效;未开启重排时 getPillProps 返回 undefined,展开无副作用 | MultiSelect.test.tsx 通过 |
| B03-9 | 已修 | `components/Combobox/Combobox.context.ts`(+`selectedValuesSet: Set<string>` 字段)、`Combobox.tsx`(`selectedValuesSet = useMemo(() => new Set(values), [values])` 进 context 与依赖)、`ComboboxOption.tsx`(`ctx.selectedValuesSet.has(value)`)、`MultiSelect.tsx`(filteredData 的 hidePickedOptions 过滤、handleOptionSubmit、renderOptions 签名改 `ReadonlySet<string>` 均查 Set) | Select/MultiSelect/TagsInput/Cascader/Autocomplete 全通过 |
| B03-10 | 已修 | `components/Tree/get-children-nodes-values/get-children-nodes-values.ts`:从已找到的 node 直接递归 collectLeaves,不再逐 child findTreeNode 全树扫描(链式树 O(n²) 消除);`components/Tree/get-all-checked-nodes/get-all-checked-nodes.ts`:新增第 4 参 `checkedSet: Set<string>`(默认 `new Set(checkedState)`,顶层一次转换、递归复用),叶子判定 `checkedSet.has`。调用方( TreeSelect/use-tree/is-node-checked)均为位置参数,兼容不变 | TreeSelect 相关用例与全量通过 |
| B03-11 | 已修 | `components/TreeSelect/TreeSelect.tsx`:解构出的 `type`/`autoComplete` 透传——单选分支 InputBase 透传 `type={type}` 与 `autoComplete={autoComplete}`;多选分支 PillsInput.Field 的 `type` 是自身可见性语义(DOM type 恒 text),故只透传 `autoComplete` | TreeSelect 渲染路径用例通过 |
| B03-12 | 已修 | `components/Cascader/Cascader.module.css`:`.searchList` 删除 `width: var(--cascader-search-width)` 死声明(该变量全仓无定义,invalid at computed-value time 退化为 auto/块级满宽;删除保持实际渲染行为不变,未改成 column-width 以免搜索列表宽度突变为固定 180px) | Cascader.test.tsx 通过 |
| B03-13 | 已修+新增测试 | `components/Cascader/Cascader.tsx`:面板项与搜索项 button 补 `aria-disabled={node.disabled \|\| undefined}`。新增测试「禁用节点带 aria-disabled,读屏可感知」 | Cascader.test.tsx 11/11 通过 |
| B03-14 | 已修 | `components/Cascader/Cascader.tsx`:handlePanelKeyDown 经 `getComputedStyle(currentTarget).direction === 'rtl'`(对齐 use-pills-reorder 的判定)翻转 ArrowLeft/ArrowRight 语义;`Cascader.module.css`:`.column` 的 `border-right` → `border-inline-end`(`:last-child` 同)、`.columnItem/.searchItem` 的 `text-align: left` → `start` | Cascader 键盘导航既有用例(LTR)通过 |
| B03-15 | 已修 | `components/TreeSelect/TreeSelectOption.tsx`:移除 Combobox.Option(role="option")上的非法 `aria-expanded`;展开语义保留在内部 expandIcon 的 role="button" + aria-label | TreeSelect 用例通过 |
| B03-16 | 跳过 | 待核实项(jsdom 无法验证 Firefox/Safari 拖拽影像),按指令不修 | — |
| B01 待核实/待裁决 ×3 | 跳过 | use-matches hooks 数量、px 乘数顺序、B01-2 嵌套语义裁决,按指令不修(B01-2 按指令采"只有根 Provider 写属性"确定化方案落地) | — |

## 备注

- B01 共 7 条全部修复(初版遗漏 B01-4,已补:localStorage 持久化链路);待核实/待裁决 3 条按指令跳过。
- B03-1/2 实现取舍:ComboboxTarget 对「事件命中的内层可编辑输入框」统一豁免 toggle(而非仅判 Target 直接子元素),MultiSelect(searchable)用 mousedown 打开(镜像 Select),TagsInput 用 wrapper onClick 兜底打开——后者是为保住既有测试「click 打开建议下拉」的编码语义(指令要求以测试为准)。
- B03-4 的 activeIndex 定位仅在 effect 实际运行时(打开/选项集变化)取当次渲染的 selectedValues,选中值变化本身不重置键盘激活态,与修复前一致,避免干扰多选打开态的鼠标/键盘导航。
- 新增测试 6 条:MultiSelect 1、TagsInput 1、Cascader 4(键盘进入面板 ×2、搜索模式进入、aria-disabled)。
