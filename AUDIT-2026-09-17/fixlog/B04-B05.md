# B04(输入控件)/ B05(选择/开关)修复日志

修复人:修复工程师(B04 + B05 批次)。日期:2026-09-17。
仓库:/Users/zhangzhengyang/Desktop/PROJECT/react-ui(工作树,未 commit/push)。

说明:B04 全部 14 条 + B04-T1、B05 全部 23 条(除指令豁免的 Rating readOnly 语义裁决)均已落地。
其中大部分改动在本轮接手前已存在于工作树(前次中断的修复运行,均未提交),本轮逐条对照审查单
核对 diff 确认无误,并补齐了缺失项(use-radial-move 越界钳制与 touchcancel、getPaginationItems 对齐、
Rating 键盘用例修正、MaskInput 类型收窄),随后完成定向验证。

## 测试总况

- 组件定向(21 个组件目录):**20 文件 / 121 用例全部通过**(vitest run,node v22.12.0)
- hooks 定向:`src/use-move` 无测试文件(vitest 报 No test files found,非失败);
  `src/use-radial-move` 为 B05-10 新增 3 用例,**3/3 通过**
- `tsc --noEmit` 过滤本批目录:0 错误(修复过程中发现并解决 1 处 MaskInput isComposing 类型收窄)
- 新增测试 11 条:PinInput ×2、Rating ×4、Pagination ×2(color 变量、getPaginationItems 一致性)、
  use-radial-move ×3;更新既有断言 2 条(getPaginationItems 分叉用例,详见 B05-16)

## B04 修复明细

| 单号 | 状态 | 改动文件与要点 | 定向测试结果 |
| --- | --- | --- | --- |
| B04-1 | 已修 | `Input/Input.tsx`:`_rightSection` 恢复普通函数调用式 `InputClearSection({...})`(该组件内部不使用 hooks,不违反 Rules of Hooks),返回真实 ReactNode 可为 undefined,`!!_rightSection` 的 mod `data-with-right-section` 与 section div 渲染恢复 truthiness 语义;空右侧内容的输入不再恒渲染 ~34px 右 section | Input/InputBase/TextInput/Textarea/JsonInput/NumberInput/PasswordInput/MaskInput/ColorInput/FileInput 全部通过,无既有测试断言 data-with-right-section 恒真 |
| B04-2 | 已修+新增测试 | `PinInput/PinInput.tsx`:内部事实源改为定长 `string[]`(toChars 按位展开、空位保留空串),setInputValue/handleKeyDown/handlePaste 均按位写入,onChange/onComplete 前再 join;受控回显仅在字符串不一致时重展开,不破坏内部空洞布局 | PinInput 14/14 通过,含新增「删除中间位空洞保留不左移」「受控回显保持空洞布局」2 条 |
| B04-3 | 已修 | `MaskInput/MaskInput.tsx`:`composingRef` + 原生 compositionstart/compositionend 监听(effect 挂载期绑定),handleChange 判 `composingRef || nativeEvent.isComposing` 直接返回,组合期间不再强写 DOM value;掩码应用逻辑抽为 `applyInputValueRef` 供 compositionend 后统一应用;`nativeEvent as InputEvent` 显式收窄解决 React ChangeEvent 泛型类型错误 | MaskInput 全部通过 |
| B04-4 | 已修 | `hooks/use-move/use-move.ts`:bindEvents/unbindEvents 补 `touchcancel` + `pointercancel`(均复用幂等的 stopScrubbing) | use-move 无既有测试;经 ColorPicker/Slider 系组件测试间接覆盖,全部通过 |
| B04-5 | 已修 | `PinInput/PinInput.module.css`:.root 按 `[data-ui-color-scheme]` 提供 light/dark 两组成对格子变量 `--pin-input-bd/--pin-input-bg`(dark: dark-4 边框/dark-6 底),.input fallback 链接入;disabled 底色改随 scheme 翻转的 `--ui-color-disabled` | PinInput 通过(CSS 不参与 jsdom 断言) |
| B04-6 | 已修 | `Input/InputClearButton/InputClearButton.tsx`:`{...others}` 提前、`style={{ pointerEvents:'all', background:'var(--input-bg)', ...others.style }}` 后置,防御性内联不再被整体覆盖 | Input 系全部通过 |
| B04-7 | 已修 | `JsonInput/JsonInput.module.css`:`.fieldSizing` 增加 `--input-font-family: var(--ui-font-family-monospace, ...)` 变量声明——autosize 时 input 只挂本类,经 Input.module.css 的 `.input { font-family: var(--input-font-family, ...) }` 消费,等宽字体恢复且不改 getStyles 挂载结构 | JsonInput 通过 |
| B04-8 | 已修 | `PasswordInput/PasswordInput.tsx`:可见性切换 ActionIcon 删除 `tabIndex={-1}`,恢复键盘/读屏可达(消费方仍可经 visibilityToggleButtonProps 覆盖) | PasswordInput 通过 |
| B04-9 | 已修 | `ColorPicker/Saturation/Saturation.tsx`:补 `aria-valuemin={0}`、`aria-valuemax={100}`,`aria-valuenow={Math.round(position.x * 100)}`(对齐 ColorSlider 的百分比语义,不再播报 0-1 裸浮点) | ColorPicker/ColorInput 全部通过 |
| B04-10 | 已修 | `FileButton/FileButton.tsx`:reset 以 useCallback 稳定,`assignRef(resetRef, reset)` 移入 useEffect 并在清理时置 null(对齐 MaskInput resetRef 写法,消除渲染期副作用与卸载残留) | FileInput/FileButton 路径测试通过 |
| B04-11 | 已修 | `NumberInput/NumberInput.tsx`:新增 `getStepDecimals`(toExponential 解析,科学计数法安全)与 `addStep`(按 step 小数位十进制整数化),increment/decrement 改用,step=0.1 不再产生 0.30000000000000004 | NumberInput 通过 |
| B04-12 | 已修 | `NumberInput/NumberInput.module.css`:步进按钮 hover 背景由恒定 `--ui-color-gray-0` 改为随 scheme 翻转的 `--ui-color-default-hover`,暗色下不再近白底浅图标 | NumberInput 通过 |
| B04-13 | 已修 | `TextInput/JsonInput/PinInput/NumberInput/MaskInput/ColorInput/ColorPicker.types` 等 7 处:`//**` 全部改 `/**`(全目录 grep 确认无残留),JSDoc/docgen 恢复 | 相关组件测试全部通过 |
| B04-14 | 已修 | `NumberInput/NumberInput.tsx`:InputWrapper 调用补 `required`/`withAsterisk` 透传,props 暴露 withAsterisk;required 显式挂回 input(解构后不再经 others) | NumberInput 通过 |
| B04-T1 | 已修(核实属实) | `ColorInput/ColorInput.tsx`:Popover opened 完全受控但未接 onClose,Escape/点击外部的关闭请求无法回流内部 state——补 `onClose={() => setDropdownOpened(false)}`,Escape 关闭生效 | ColorInput/ColorPicker 通过 |

B04 待核实 T2(MaskInput separate 模式同值赋值)、T3(PinInput 粘贴全非法清空)按指令不修。

## B05 修复明细

| 单号 | 状态 | 改动文件与要点 | 定向测试结果 |
| --- | --- | --- | --- |
| B05-1 | 已修+新增测试 | `Pagination/PaginationRoot.tsx`:varsResolver 与 CssVariables 类型改为写 `--pagination-control-bg/--pagination-control-color`,与 Pagination.module.css `.control[data-active]` 读取端对齐,color/autoContrast 恢复生效 | Pagination 通过,含新增「color 写入 CSS 消费端变量、未传 color 不写入」用例 |
| B05-2 | 已修+新增测试 | `Rating/Rating.tsx`:radio click 路径经 `getValueFromPointer` 按指针位置提交分数值(fractions 等分单星),`pointerCommitRef` 标记让紧跟的整星 change 跳过;键盘合成 click(detail=0,如空格)退回整星;clearable 以「分数值 === baseValue」判定清零;键盘 change 路径保持整星 | Rating 9/9 通过,含新增半星提交、键盘整星、clearable 分数清零、已勾选星另半改值 4 条 |
| B05-3 | 已修 | `Slider/Slider.module.css`:`.thumb`(连带 `.mark`/`.markLabel`)补 `pointer-events: none`,可见装饰层不再拦截透明 input 的指针事件,按住手柄拖拽恢复 | Slider 通过 |
| B05-4 | 已修 | `Checkbox/CheckboxCard.tsx`:props 声明 disabled,`resolvedDisabled = disabled ?? ctx?.disabled` 消费组级禁用;handleClick 入口拦截并挂 `disabled`/aria 语义(对齐 RadioCard) | Checkbox 通过 |
| B05-5 | 已修 | `Rating/Rating.tsx`:`getStyles('starSymbol')` 产物以 clsx/style 合并进两层星形,删除与 CSS 重复的内联 color,classNames/styles.starSymbol 恢复生效;`Slider.tsx`/`RangeSlider.tsx` 的 bar/thumb/mark 定位样式同样与 getStyles 产物合并不整体覆盖 | Rating/Slider/RangeSlider 全部通过 |
| B05-6 | 已修 | `Rating/Rating.tsx`:星形 radio aria-label 统一中文 `${starValue} 星`,消除组内「评分」+「N star」中英混用 | Rating 通过 |
| B05-7 | 已修 | `CheckboxGroup/RadioGroup/SwitchGroup/ChipGroup/PaginationRoot`:context value 统一 useMemo 稳定身份;ChipGroup 的 isChipSelected/handleChange 先 useCallback(依赖 _value/setValue) | 五个组件测试全部通过 |
| B05-8 | 已修 | `RangeSlider/RangeSlider.tsx`:新增 keydown 值快照(keyDownValueRef)+ handleInputKeyUp,值变化时补发 onChangeEnd 并同步焦点快照避免 blur 重复触发,对齐 Slider 的 keyup 语义 | RangeSlider 通过 |
| B05-9 | 已修 | `RangeSlider/RangeSlider.tsx`:补 `name`(渲染 `${name}[0]/${name}[1]` 两个 hidden input 参与表单序列化)与 `thumbLabel`(string 或 [string,string],缺省回落内置可访问名) | RangeSlider 通过 |
| B05-10 | 已修+新增测试 | `hooks/use-radial-move/use-radial-move.ts`:normalizeRadialValue 重写——ceil(x)>=x 恒真使原 low 分支死代码,ceil 网格越界(如 step=7、358°→364)时回落 round 网格值,360/浮点略超 360 回绕为 0,值域钳回 [0,360);`AngleSlider/AngleSlider.tsx`:Home/End 分支补 preventDefault | 新增 use-radial-move.test.ts 3/3 通过(含 step=7 越界回归用例);AngleSlider 通过 |
| B05-11 | 已修 | `Switch/SegmentedControl/Slider/RangeSlider` 各 .module.css:统一补 `:has(.input:focus-visible)` 焦点描边规则(Switch/Slider/RangeSlider 落 track,SegmentedControl 落 control),键盘焦点可视(对齐 Rating 既有模式) | 四组件测试全部通过(CSS 不参与 jsdom 断言) |
| B05-12 | 已修 | `Slider.tsx`/`RangeSlider.tsx`:接入 useDirection 读容器 dir,`invertPhysicalAxis = inverted !== (dir==='rtl')` 统一驱动 bar/thumb/mark 物理起点翻转,RangeSlider 的 useMove dir 参数同步传入;`Switch.module.css`:thumb 改 `inset-inline-start` 并在选中态按 [dir='rtl'] 取反位移 | Slider/RangeSlider/Switch LTR 既有用例全部通过 |
| B05-13 | 已修 | `SegmentedControl/SegmentedControl.tsx`:`name ?? React.useId()` 默认生成组名,浏览器原生 roving(方向键切换)生效 | SegmentedControl 通过 |
| B05-14 | 已修 | `SegmentedControl/SegmentedControl.tsx`:stylesNames 移除从未渲染的 `controlActive`(全仓无引用残留);transitionDuration 文档改为「作用域控件背景/颜色过渡」;激活态语义由 `.control[data-active]` 承载 | SegmentedControl 通过 |
| B05-15 | 已修 | `Pagination/Pagination.module.css`:补 responsive 实现——`.root[data-layout='responsive']` 设 container-type: inline-size、默认隐藏 .label,`@container (max-width: 24rem)` 内隐藏 .items 显示 .label,文档承诺的容器查询切换生效 | Pagination 通过(CSS 布局不参与 jsdom 断言) |
| B05-16 | 已修+新增测试+更新断言 | `Pagination/Pagination.tsx`:getPaginationItems 按 usePagination(组件实际算法)重写对齐——totalPageNumbers 边界、leftSiblingIndex 下限(_startValue+boundaries-1)、左右溢出 item 数(+2/+boundaries+1+2·siblings)与组件一致,补可选 startValue 参数与 siblings/boundaries NaN 归一;**同步更新 2 条既有 utility 断言**(total=20,active=2 → [1..5,'dots',20]、active=19 → [1,'dots',16..20],原断言锁定的是分叉复制品的错误输出,审查建议明示同步更新),另新增「工具输出与组件渲染一致」回归用例 | Pagination 通过,含一致性用例 |
| B05-17 | 已修 | `Pagination/PaginationRoot.tsx`:新增 normalizeCount,siblings/boundaries 传入 usePagination 前钳为非负整数(非有限数回落默认 1),对齐 total/startValue 的防御策略(根因 use-pagination 属 hooks 包未在授权范围,于组件边界归一) | Pagination 通过 |
| B05-18 | 已修 | `Pagination/Pagination.module.css`:.root 补基础声明 `--pagination-control-size: var(--pagination-control-size-md)`,缺省档与文档 @default 'md' 一致 | Pagination 通过 |
| B05-19 | 已修 | `Chip/ChipGroup.tsx`:删除死参数 name(props 与解构;Chip 为 button 无可挂 name 的表单控件) | Chip 通过 |
| B05-20 | 已修 | `Radio/RadioIndicator.tsx`:接线 useStyles/getStyles('indicator')(classNames/styles/className/style/vars 生效),size/color 经 varsResolver 映射 `--radio-indicator-size/--radio-indicator-color` 消费,不再作为未知属性泄漏到 DOM span | Radio 通过 |
| B05-21 | 已修 | `Radio/Radio.tsx`:varsResolver 的 `'--radio-icon-color': iconColor ? getThemeColor(iconColor, theme) : undefined`,与 Checkbox 对齐,主题键不再生成非法 CSS 值 | Radio 通过 |
| B05-22 | 已修 | `Radio/RadioGroup.tsx`:根 div 补 `role="radiogroup"`,RadioCard(role=radio)组合下读屏可感知分组(对齐 Rating) | Radio 通过 |
| B05-23 | 已修 | `hooks/use-move/use-move.ts`(B04-4 同修)、`hooks/use-radial-move/use-radial-move.ts`:beginTracking/endTracking/cleanupRef 三处补 touchcancel + pointercancel → stopScrubbing/endTracking;`Slider.tsx` 原生 input 补 onTouchCancel={handleChangeEnd} | hooks 新增用例通过;Slider/RangeSlider/AngleSlider/ColorPicker 组件测试全部通过 |

B05 待核实 2(Rating readOnly 经 disabled 实现)属语义裁决,按指令不修。

## 备注

- B04-2/B05-2/B05-3 等条目与上轮报告「声称已修」相符但代码未落地(修复被冲掉),本轮落地后已补回归测试锁定(PinInput 空洞 ×2、Rating 半星 ×4、Slider pointer-events 为纯 CSS)。
- Rating 键盘用例取舍:React 对 radio 的 onChange 由 click 派生,`fireEvent.change` 不进 React 合成系统(jsdom 实测 0 次调用);键盘语义用例改为 detail=0 的 click(空格激活路径,退回整星),方向键原生 roving 在浏览器侧经同一 onChange 处理器提交整星。
- B05-16 按审查建议同步更新了 2 条锁定分叉行为的 utility 断言(其余 2 条旧断言与新算法输出本就一致),并新增「工具与渲染一致」回归用例防止再分叉;组件渲染行为未变。
- B05-17 的 use-pagination.ts 属并行范围外的 hooks 包,归一化在 PaginationRoot 传入前完成,行为等价。
