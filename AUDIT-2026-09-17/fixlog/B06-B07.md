# B06-B07 修复日志(数据展示 / 布局与导航)

修复日期:2026-09-17。范围:B06 全部 18 条 + B07 全部 14 条;待核实 3 条(B06-18 核实属实已修,B07-T1/T2 按指令不修)。
测试编码冲突:0 条(无跳过)。仅改动 B06/B07 清单组件目录内文件,未触碰其他目录。

定向测试(node v22.12.0, vitest run 分步):
- Batch 1 `Tabs Table DataTable Tree Accordion Collapse`:4 files / 38 tests 全过
- Batch 2 `Stepper Timeline Progress RingProgress Highlight Watermark Spoiler Marquee`:6 files / 26 tests 全过
- Batch 3 `Grid SimpleGrid ScrollArea Splitter Image AppShell Container OverflowList`:6 files / 32 tests 全过
- 补充定向 `Indicator Descriptions Skeleton Kbd Divider NavLink Cascader SemiCircleProgress RollingNumber BackgroundImage`:7 files / 49 tests 全过
- `tsc --noEmit`:本次改动文件无类型错误(CodeHighlight/dates/schedule 的既有错误与本次无关,未触碰)

新增测试:3 个(Tabs roving tabindex 收敛 ×1,Splitter 多指忽略 ×1、pointercancel 清理 ×1);更新 1 个(Splitter 拖拽 mouse→pointer 事件模拟,测试意图不变)。

## B06(数据展示)

| 单号 | 状态 | 改动文件与要点 | 定向测试 |
|---|---|---|---|
| B06-1 | 已修 | `Tabs/TabsTab.tsx`:useIsomorphicEffect 去掉 `activeValue !== undefined` 早退;激活值存在时按 `isActive` 纠偏命令式写入的 tabIndex,消除首个 tab 残留 `tabIndex=0` 的双入口;`Tabs/Tabs.test.tsx` 新增"无 defaultValue 激活第二个 tab 后仅一个 tabindex=0 入口"用例 | Batch 1(Tabs.test 9 tests 全过) |
| B06-2 | 已修 | `Collapse/Collapse.tsx`:duration=0 早退分支补 useIsomorphicEffect(ref 记录 prevExpanded,跳过首挂),expanded 变化时同步触发 `onTransitionStart?.()/onTransitionEnd?.()`,与 hooks 层 finalizeTransition 契约一致 | Batch 1(Collapse 无测试文件,Accordion 依赖 Collapse 的测试全过) |
| B06-3 | 已修 | `Tree/filter-tree-data/filter-tree-data.ts`:统一"只保留命中链"语义——节点命中但子节点均未命中时剥离 children key(不写 `children: []`,避免空展开箭头/异步 hasChildren 失效);部分子命中时保留命中子集;叶子原样保留。Cascader(复用该函数)11 tests 全过 | Batch 1 + Cascader 补充定向 |
| B06-4 | 已修 | `Tree/use-tree.ts`:initialize 内对生成的 expandedState(逐 key 比 boolean)与 checkedState(逐项比)与当前值浅比较,同值新身份不提交,消除内联 data 时受控 onChange 连续调用/渲染循环 | Batch 1 |
| B06-5 | 已修 | `Tree/TreeNode.tsx` + `Tree/FlatTreeNode.tsx`:Space 分支改互斥(checkOnSpace 优先,勾选时不再同时 toggleExpanded) | Batch 1 |
| B06-6 | 已修 | 删除 `Tree/is-node-indeterminate/is-node-indeterminate.ts`(全仓零引用死代码,controller.isNodeIndeterminate 走 use-tree.ts 的 checkedNodesMap);目录一并移除 | Batch 1(编译通过) |
| B06-7 | 已修 | `Highlight/Highlight.tsx`:接入 useStyles,getStyles('root') 挂根元素、getStyles('highlight') 传给 Mark(classNames/styles/unstyled/attributes 全部生效,'highlight' 样式名启用) | Batch 2(Highlight 4 tests) |
| B06-8 | 已修 | `Stepper/Stepper.tsx`:删除无 role div 上无效的 `aria-orientation` | Batch 2(Stepper 6 tests) |
| B06-9 | 已修 | `Stepper/Stepper.module.css` + `Stepper.tsx`:stepIcon `background-color` → `--ui-color-body`、border → `--ui-color-default-border`;`--stepper-separator-color`(CSS 默认值与 varsResolver 双处)→ `--ui-color-default-border`,暗色随色制翻转 | Batch 2 |
| B06-10 | 已修 | `RingProgress/RingProgress.tsx`:sections 值经 Number.isFinite 归一(NaN/Infinity→0),杜绝 "M NaN NaN" path;根元素补 `role="progressbar"` + aria-valuemin/max=0/100 + 钳制 aria-valuenow。`SemiCircleProgress/SemiCircleProgress.tsx`:value 同样归一,补 progressbar 语义 | Batch 2(RingProgress 3 tests)+ SemiCircleProgress 补充定向 |
| B06-11 | 已修 | `Indicator/get-position-variables/get-position-variables.ts` + `Indicator.tsx` + `Indicator.module.css`:`--indicator-left/right` → `--indicator-inline-start/end`,CSS 侧改 `inset-inline-start/end`(translate 相对自身尺寸居中,无需翻转);RTL 下 start/end 语义正确 | Indicator 补充定向(11 tests) |
| B06-12 | 已修 | `Tabs/TabsList.tsx`:引入 useDirection,RTL 时水平 ArrowLeft/Right 分支互换(APG);`Tabs.module.css`:vertical 变体 border-right/margin-right → border-inline-end/margin-inline-end(default/outline 两处) | Batch 1 |
| B06-13 | 已修 | `Tabs/TabsList.tsx`:cloneElement 改 `key: child.key ?? index`,保留消费者 key,动态增删/重排不再引发后续 tab 全量 remount | Batch 1 |
| B06-14 | 已修 | `Marquee/Marquee.tsx`:重复组 `aria-hidden={index > 0 \|\| undefined}`,首组保留语义 | Batch 2(Marquee 无测试文件,编译通过) |
| B06-15 | 已修 | `Marquee.module.css` + `Skeleton.module.css`:补 `@media (prefers-reduced-motion: reduce) { animation: none }` | Batch 2(Skeleton 4 tests 过) |
| B06-16 | 已修 | `RollingNumber/RollingNumber.tsx`:withLiveRegion 时追加 `<VisuallyHidden>{accessibleValue}</VisuallyHidden>` + `aria-live="polite"`(aria-label 保留作静态描述),live 播报基于文本内容变更 | RollingNumber 补充定向(无测试文件,编译通过) |
| B06-17 | 已修 | `Descriptions/Descriptions.module.css`:选择器限定为 `.root:where([data-layout='vertical']):not([data-bordered]) .value > div:first-child`,horizontal 下不再污染消费者 div 内容(mod 序列化确认 data-layout 属性存在) | Descriptions 补充定向(6 tests) |
| B06-18 | 已修(核实属实) | `Watermark/Watermark.tsx`:读代码确认测量用 `${fontSize}px sans-serif`、SVG `<text>` 无 font-family,数据 URL 不继承页面字体、默认字体(常为 serif)与实测不一致可致裁切;`<text>` 显式补 `font-family="sans-serif"` 与测量对齐 | Batch 2(编译通过) |

## B07(布局与导航)

| 单号 | 状态 | 改动文件与要点 | 定向测试 |
|---|---|---|---|
| B07-1 | 已修 | `Splitter/SplitterResizer/SplitterResizer.tsx`:拖拽整体改 Pointer Events(pointerdown/pointermove/pointerup/pointercancel + setPointerCapture try/catch 兜底),pointerId 校验(多指忽略),pointercancel/卸载双清理移除 document 监听,触摸中断不再残留"无按键拖动";`Splitter.module.css`:`.resizer` 补 `touch-action: none`;`Splitter.test.tsx` 补 jsdom PointerEvent polyfill,拖拽用例改 pointer 事件 + 新增多指忽略/pointercancel 清理 2 用例 | Batch 3(Splitter 5 tests 全过) |
| B07-2 | 已修 | `ScrollArea/ScrollArea.module.css`:thumb/轨道颜色改可主题化变量(--scrollarea-thumb-color/-hover-color/track-hover-color),light 用默认值,`[data-ui-color-scheme='dark'] &` 祖先选择器覆盖为白色系 rgba(与 Paper/Card 模式一致),轨道 hover 走语义 token `--ui-color-default-hover` | Batch 3(ScrollArea 14 tests) |
| B07-3 | 已修 | `ScrollArea/ScrollArea.module.css`:删除 corner 永不可达的 `&:where([data-hovered])` 死规则(组件侧从无 data-hovered 输出,取"删除"方案) | Batch 3 |
| B07-4 | 已修 | `ScrollArea/ScrollArea.tsx`:ScrollAreaAutosize 的 onOverflowChange 经 ref 转发,handleOverflowCheck 依赖收敛为 [],effect 依赖收敛为 [viewportObserverElement, handleOverflowCheck],内联回调不再触发 ResizeObserver 反复重挂 | Batch 3 |
| B07-5 | 已修 | `Grid/GridCol.tsx`:offset 钳位到 [0, columns-1](负值按 0),grid-column 分支与 grow 的 marginInlineStart 分支统一使用钳位结果,offset≥columns 不再创建隐式列溢出 | Batch 3(Grid 2 tests,`offset={2}` 期望 `3 / span 4` 保持) |
| B07-6 | 已修 | `Grid/GridCol.tsx`:grow 模式 flexBasis/marginInlineStart 的列数改用 `var(--grid-cols, columns)` 表达(GridVariables 已按断点生成该变量),响应式 cols 在 grow 下不再静默失效 | Batch 3 |
| B07-7 | 已修 | `Image/Image.tsx`:新增 fallbackError 态,fallbackSrc 加载失败后链式切到 fallback 节点(有 fallback 时);fallbackSrc 的 img 透传消费者 onLoad/onError;src 变化时同步重置 fallbackError | Batch 3(Image 4 tests) |
| B07-8 | 已修 | `BackgroundImage/BackgroundImage.tsx`:`url("${src.replace(/"/g, '\\"')}")` 加引号+转义,含空格/括号/引号的 src 不再生成非法 CSS | BackgroundImage 补充定向(编译通过) |
| B07-9 | 已修 | `NavLink/NavLink.tsx`:hasChildren 时输出 `aria-expanded={_opened}` | NavLink 补充定向(8 tests) |
| B07-10 | 已修 | `Container/Container.module.css`:grid 分支补 `&:where([data-fluid]) { grid-template-columns: 1fr 100% 1fr }`,fluid + strategy='grid' 不再被 --container-size 限宽 | Container 补充定向(Batch 3) |
| B07-11 | 已修 | `Scroller/Scroller.module.css`:start/end 控件在 `[dir='rtl'] &` 下翻转两处 linear-gradient 方向,遮罩浓侧恒朝内容区 | Scroller(无测试文件,编译通过) |
| B07-12 | 已修 | `AppShell/AppShell.module.css`:去掉 navbar/aside 无效的 width transition,collapse 过渡改在轨道层面(`.root` 补 `transition: grid-template-columns 0.2s ease`);旧浏览器不支持轨道插值时整体退化为瞬时切换,收起/展开保持对称 | Batch 3(AppShell 3 tests) |
| B07-13 | 已修 | `Divider/Divider.module.css`:`--divider-color` 补 `[data-ui-color-scheme='dark'] &` → `--ui-color-dark-4`;`Kbd/Kbd.module.css`:边框色改 `--kbd-border-color` 变量并补 dark 覆盖 → `--ui-color-dark-4`(与 Paper/Card 模式对齐) | Kbd/Divider 补充定向(Kbd/Divider tests 过) |
| B07-14 | 已修 | `OverflowList/OverflowList.tsx`:首个重测 effect 依赖纳入 maxVisibleItems 与 getSpacing(gap) 解析值(gap 变化不改容器尺寸,ResizeObserver 不触发),顺带修复 gap 未解构的类型错误 | Batch 3(编译通过) |

## 跳过清单(待核实,按指令不修)

| 单号 | 状态 | 原因 |
|---|---|---|
| B07-T1 | 跳过 | Splitter 键盘方向语义:当前实现自洽(注释明确、与桌面软件一致),指令明确不修 |
| B07-T2 | 跳过 | Text truncate='start' 的 direction:rtl hack:期望视觉行为未在测试/文档编码,指令明确不修 |
