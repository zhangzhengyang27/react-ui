import React from 'react';

interface DocsFooterProps {
  withNavbar?: boolean;
}

/**
 * 文档页脚组件（占位）。
 * 原 website 版本返回 null，此处保持一致。
 */
const DocsFooter: React.FC<DocsFooterProps> = () => {
  return null;
};

export default DocsFooter;
