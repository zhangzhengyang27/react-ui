import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useCallbackRef

useCallbackRef Hook 的使用说明。

## 使用示例

<Demo
    title="基础用法"
    description="useCallbackRef 基础使用示例"
    themeToggle
    rtl
    code={`import { useCallbackRef } from '@react-ui/hooks'

export default function Example() {
    const result = useCallbackRef(callback)
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useCallbackRef" />
</Demo>

## API

<HookApiTable hook="useCallbackRef" />
