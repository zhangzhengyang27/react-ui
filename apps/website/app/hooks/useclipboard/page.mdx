import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useClipboard

useClipboard Hook 的使用说明。

## 使用示例

<Demo
    title="基础用法"
    description="useClipboard 基础使用示例"
    themeToggle
    rtl
    code={`import { useClipboard } from '@react-ui/hooks'

export default function Example() {
    const result = useClipboard({
        // 配置选项
    })
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useClipboard" />
</Demo>

## API

<HookApiTable hook="useClipboard" />
