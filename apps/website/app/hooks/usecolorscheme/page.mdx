import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useColorScheme

根据用户系统偏好获取当前颜色方案

## 使用示例

<Demo
    title="基础用法"
    description="useColorScheme 基础使用示例"
    themeToggle
    rtl
    code={`import { useColorScheme } from '@react-ui/hooks'

export default function Example() {
    const result = useColorScheme()
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useColorScheme" />
</Demo>

## API

<HookApiTable hook="useColorScheme" />
