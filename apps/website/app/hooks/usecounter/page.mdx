import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useCounter

自定义计数器 Hook，提供计数功能及操作方法

## 使用示例

<Demo
    title="基础用法"
    description="useCounter 基础使用示例"
    themeToggle
    rtl
    code={`import { useCounter } from '@react-ui/hooks'

export default function Example() {
    const result = useCounter(0)
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useCounter" />
</Demo>

## API

<HookApiTable hook="useCounter" />
