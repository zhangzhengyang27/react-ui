import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useMergedRef

合并多个 ref 的 hook，返回稳定的 callback。 对齐 mantine useMergedRef。

## 使用示例

<Demo
    title="基础用法"
    description="useMergedRef 基础使用示例"
    themeToggle
    rtl
    code={`import { useMergedRef } from '@react-ui/hooks'

export default function Example() {
    const result = useMergedRef()
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useMergedRef" />
</Demo>

## API

<HookApiTable hook="useMergedRef" />
