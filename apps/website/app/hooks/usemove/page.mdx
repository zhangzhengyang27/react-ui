import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useMove

useMove Hook 的使用说明。

## 使用示例

<Demo
    title="基础用法"
    description="useMove 基础使用示例"
    themeToggle
    rtl
    code={`import { useMove } from '@react-ui/hooks'

export default function Example() {
    const result = useMove(onChange)
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useMove" />
</Demo>

## API

<HookApiTable hook="useMove" />
