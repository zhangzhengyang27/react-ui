import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useResizeObserver

useResizeObserver Hook 的使用说明。

## 使用示例

<Demo
    title="基础用法"
    description="useResizeObserver 基础使用示例"
    themeToggle
    rtl
    code={`import { useResizeObserver } from '@react-ui/hooks'

export default function Example() {
    const result = useResizeObserver({
        // 配置选项
    })
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useResizeObserver" />
</Demo>

## API

<HookApiTable hook="useResizeObserver" />
