import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useScrollSpy

useScrollSpy Hook 的使用说明。

## 使用示例

<Demo
    title="基础用法"
    description="useScrollSpy 基础使用示例"
    themeToggle
    rtl
    code={`import { useScrollSpy } from '@react-ui/hooks'

export default function Example() {
    const result = useScrollSpy({
        // 配置选项
    })
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useScrollSpy" />
</Demo>

## API

<HookApiTable hook="useScrollSpy" />
