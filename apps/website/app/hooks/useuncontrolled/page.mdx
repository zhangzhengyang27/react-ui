import { HookApiTable } from '@/components/HookApiTable'
import { Demo } from '@/components/Demo'
import { HookExample } from '@/components/HookExamples'

# useUncontrolled

用于管理非受控组件的状态，支持受控与非受控模式的切换

## 使用示例

<Demo
    title="基础用法"
    description="useUncontrolled 基础使用示例"
    themeToggle
    rtl
    code={`import { useUncontrolled } from '@react-ui/hooks'

export default function Example() {
    const result = useUncontrolled({
        // 配置选项
    })
    return <div>{JSON.stringify(result)}</div>
}`}
>
    <HookExample hook="useUncontrolled" />
</Demo>

## API

<HookApiTable hook="useUncontrolled" />
