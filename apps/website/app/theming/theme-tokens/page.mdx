---
title: 主题令牌
---

import { ThemeTokens } from '@/components/ThemeTokens'

# 主题令牌速查

以下令牌由 `packages/ui/src/core/MantineProvider/default-theme.ts` 与 `default-colors.ts` 自动生成，无需手动维护。通过 `Provider` 的 `theme` 属性覆盖对应字段即可全局自定义。

<ThemeTokens />

## 下一步

- 了解 [主题对象](/theming/theme-object) 完整类型
- 查看 [颜色系统](/theming/colors)
- 返回 [主题概览](/theming)
