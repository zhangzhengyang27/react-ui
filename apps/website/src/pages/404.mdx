import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.NotFound);

# 404

页面未找到。如果某个文档页面链接到了此页面，请[创建 issue](https://github.com/xiaoye/react-ui/issues/new/choose) 告知我们。否则，请返回[首页](/)。
