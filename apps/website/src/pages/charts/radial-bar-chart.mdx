import { RadialBarChartDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.RadialBarChart);

## 用法

`RadialBarChart` 基于 [RadialBarChart recharts 组件](https://recharts.org/en-US/api/RadialBarChart)：

<Demo data={RadialBarChartDemos.usage} />

## 更改颜色

你可以在 `data` 的 `color` 属性中引用主题颜色或使用任意有效的 CSS 颜色：

<Demo data={RadialBarChartDemos.color} />

## 图例

要显示图例，设置 `withLegend` 属性：

<Demo data={RadialBarChartDemos.legend} />

## 标签

要显示标签，设置 `withLabels` 属性：

<Demo data={RadialBarChartDemos.labels} />

## 隐藏提示框

要隐藏提示框，设置 `withTooltip={false}` 属性：

<Demo data={RadialBarChartDemos.noTooltip} />
