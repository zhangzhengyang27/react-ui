import { SparklineDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Sparkline);

## 用法

`Sparkline` 是 [AreaChart](/charts/area-chart) 的简化版本，可用于在较小空间内展示单组数据。

<Demo data={SparklineDemos.usage} />

## 根据颜色方案改变区域颜色

你可以在 `color` 属性中使用 CSS 变量。要定义随颜色方案变化的 CSS 变量，可使用 [light/dark mixins](/styles/postcss-preset/#dark-and-light-mixins)
或 [light-dark 函数](/styles/postcss-preset/#light-dark-function)。例如，一个区域在浅色模式下为深橙色，在深色模式下为柠檬色：

<Demo data={SparklineDemos.colorSchemeColor} />

## 趋势颜色

使用 `trendColors` 属性替代 `color`，根据趋势改变图表颜色。该属性接受一个包含 `positive`、`negative` 和 `neutral` 的对象：

- `positive` - 正值趋势颜色（`data` 数组中首个值小于最后一个值）
- `negative` - 负值趋势颜色（`data` 数组中首个值大于最后一个值）
- `neutral` - 中性趋势颜色（首尾两个值相等）

`neutral` 为可选；若未提供，颜色将与 `positive` 相同。

<Demo data={SparklineDemos.trendColors} />
