import { AccordionDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Accordion);

## 用法

Accordion 允许用户展开和折叠内容区域。
它通过最初只显示区域标题并在交互时显示内容，
帮助在有限空间内管理大量信息。

Accordion 常用于：
- FAQ 区域：将问题显示为标题，点击后显示答案
- 表单：将长表单组织成多个区域，例如个人信息、配送和支付
- 菜单：侧边栏或移动视图中的嵌套导航

<Demo data={AccordionDemos.configurator} />

## order 属性

`order` 属性（本页所有示例均使用）设置 `Accordion.Control` 根元素的标题级别。
根据 [WAI-ARIA 建议](https://www.w3.org/WAI/ARIA/apg/patterns/accordion/)，
需要使用 `h2`-`h6` 标题级别以正确适应页面大纲。

本页所有示例均使用 `order={3}`，这意味着所有
`Accordion.Control` 的 `button` 元素都包裹在 `h3` 标签中（文档章节使用 `h2` 标签）。

`order` 属性不是由库强制要求的，但如果你的应用
需要满足无障碍标准，则是必需的。

## 修改 chevron

使用 `chevron` 属性更改 chevron 图标。设置 `chevron` 后，
`chevronIconSize` 属性将被忽略。要移除 chevron 图标，请使用 `chevron={null}`。

要自定义 chevron 样式，请使用 [Styles API](/styles/styles-api/) 和
[data-rotate](/styles/data-attributes/) 属性。当项打开且未设置 `disableChevronRotation` 属性时，
会设置此属性。

自定义 chevron 图标及旋转样式示例：

<Demo data={AccordionDemos.chevron} />

## 自定义控制标签

你可以将任何 React 节点用作 `Accordion.Control` 组件的标签。
在 `Accordion.Control` 中使用嵌套元素时，建议设置
`aria-label` 属性，以便屏幕阅读器可以访问该控件。

<Demo data={AccordionDemos.label} />

## 使用图标

使用 `icon` 属性在 `Accordion.Control` 的左侧区域显示任意元素：

<Demo data={AccordionDemos.icons} />

## 修改过渡

要更改过渡持续时间，请设置 `transitionDuration` 属性：

<Demo data={AccordionDemos.customTransitions} />

要禁用过渡，请将 `transitionDuration` 设置为 0：

<Demo data={AccordionDemos.disableTransitions} />

## 默认打开项

对于 `multiple={false}`，将 `defaultValue` 设置为字符串：

```tsx
import { Accordion } from '@react-ui/ui';

function Demo() {
  // 默认打开第二项
  return (
    <Accordion defaultValue="item-2">
      <Accordion.Item value="item-1">{/* item-1 */}</Accordion.Item>
      <Accordion.Item value="item-2">{/* item-2 */}</Accordion.Item>
    </Accordion>
  );
}
```

对于 `multiple={true}`，将 `defaultValue` 设置为字符串数组：

```tsx
import { Accordion } from '@react-ui/ui';

function Demo() {
  // 默认同时打开两项
  return (
    <Accordion multiple defaultValue={['item-1', 'item-2']}>
      <Accordion.Item value="item-1">{/* item-1 */}</Accordion.Item>
      <Accordion.Item value="item-2">{/* item-2 */}</Accordion.Item>
    </Accordion>
  );
}
```

## 控制打开状态

对于 `multiple={false}`，将 `value` 设置为字符串：

```tsx
import { useState } from 'react';
import { Accordion } from '@react-ui/ui';

function Demo() {
  const [value, setValue] = useState<string | null>(null);

  return (
    <Accordion value={value} onChange={setValue}>
      <Accordion.Item value="item-1">{/* item-1 */}</Accordion.Item>
      <Accordion.Item value="item-2">{/* item-2 */}</Accordion.Item>
    </Accordion>
  );
}
```

对于 `multiple={true}`，将 `value` 设置为字符串数组：

```tsx
import { useState } from 'react';
import { Accordion } from '@react-ui/ui';

function Demo() {
  const [value, setValue] = useState<string[]>([]);

  return (
    <Accordion multiple value={value} onChange={setValue}>
      <Accordion.Item value="item-1">{/* item-1 */}</Accordion.Item>
      <Accordion.Item value="item-2">{/* item-2 */}</Accordion.Item>
    </Accordion>
  );
}
```

## 组合控件

在 `Accordion.Control` 内放置按钮或链接是使用 Accordion 时的常见错误。
`Accordion.Control` 的根元素是 `button`。禁止在交互元素内放置其他交互元素——
如果你尝试实现以下组件，React 会抛出 DOM 验证错误：

```tsx
import { Accordion } from '@react-ui/ui';

// ❌ 错误用法：不要这样做
function Demo() {
  return (
    <Accordion.Item value="item-1">
      <Accordion.Control>
        <Group>
          <span>Control 1</span>
          <button>My action</button>
        </Group>
      </Accordion.Control>
      <Accordion.Panel>Panel 1</Accordion.Panel>
    </Accordion.Item>
  );
}
```

不要在 `Accordion.Control` 内放置交互元素，而是将其渲染在控件旁边。
例如，你可以在原始控件的右侧添加 [ActionIcon](/core/action-icon/) 或 [Menu](/core/menu/)。
如果你需要在 `Accordion.Control` 上方显示交互元素，请改用 `position: absolute`。

<Demo data={AccordionDemos.sideControls} />

## 禁用项

在 `Accordion.Control` 组件上设置 `disabled` 属性以禁用它。
禁用项后，用户无法使用鼠标或键盘激活它们，
并且方向键导航会跳过它们：

<Demo data={AccordionDemos.disabled} />

## 无样式 Accordion

在 Accordion 组件上设置 `unstyled` 属性以移除所有非必要的
库样式。使用 `unstyled` 属性可以通过
[Styles API](/styles/styles-api/) 为组件设置样式，而无需覆盖任何样式。

<Demo data={AccordionDemos.unstyled} />

<StylesApiSelectors component="Accordion" />

<Demo data={AccordionDemos.stylesApi} />

使用 [Styles API](/styles/styles-api/) 自定义 Accordion 样式的示例：

<Demo data={AccordionDemos.customize} />

## TypeScript 类型

从 `@react-ui/ui` 导出的 `AccordionProps` 类型是一个泛型，接受一个布尔类型
来描述 `multiple` 状态：

```tsx
import type { AccordionProps } from '@react-ui/ui';

type MultipleAccordionProps = AccordionProps<true>;
type DefaultAccordionProps = AccordionProps<false>;
```

## 可访问性

Accordion 组件实现了 [WAI-ARIA 可访问性模式](https://www.w3.org/WAI/ARIA/apg/patterns/accordion/)。

## 键盘交互

<KeyboardEventsTable
  data={[
    { key: 'ArrowDown', description: '将焦点移到下一个项目' },
    { key: 'ArrowUp', description: '将焦点移到上一个项目' },
    { key: 'Home', description: '将焦点移到第一个项目' },
    { key: 'End', description: '将焦点移到最后一个项目' },
    {
      key: 'Space/Enter',
      description: '切换聚焦项目的打开状态',
    },
  ]}
/>
