import { AffixDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Affix);

## 用法

`Affix` 在 [Portal](/core/portal) 组件内渲染一个固定定位的 div 元素。
使用它来将元素固定在屏幕上的任意位置，例如返回顶部按钮：

<Demo data={AffixDemos.usage} />
