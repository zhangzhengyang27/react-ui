import { BreadcrumbsDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Breadcrumbs);

## 用法

`Breadcrumbs` 组件接受任意数量的 React 节点作为子元素，
并在它们之间添加指定的分隔符（默认为 `/`）：

<Demo data={BreadcrumbsDemos.usage} />
