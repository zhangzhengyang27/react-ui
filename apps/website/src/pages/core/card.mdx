import { CardDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Card);

## 用法

`Card` 是 [Paper](/core/paper/) 组件的包装器，带有一些额外的样式和 `Card.Section`
组件，可让你将卡片分成多个区域。如果你不需要区域，可以直接使用 [Paper](/core/paper/) 组件。

<Demo data={CardDemos.usage} />

## 水平方向

<Demo data={CardDemos.horizontal} demoProps={{ defaultExpanded: false }} />

## 多态组件

Card 是一个[多态组件](/guides/polymorphic/)，你可以更改其根元素：

<Demo data={CardDemos.link} />

## Card.Section

`Card.Section` 是一个特殊组件，用于移除 Card 对其子元素的内边距，而其他元素仍保留水平间距。
`Card.Section` 的工作方式如下：

- 如果该组件是 Card 的第一个子元素，则它具有负的上、左、右外边距
- 如果它是 Card 的最后一个子元素，则它具有负的下、左、右外边距
- 如果它在中间，则只有左、右外边距为负

```tsx
import { Card, Text } from '@react-ui/ui';

function Demo() {
  return (
    <Card padding="xl">
      {/* 上、右、左外边距为负 – -1 * theme.spacing.xl */}
      <Card.Section>第一部分</Card.Section>

      {/* 不在 Card.Section 内的内容相对于 Card 具有 theme.spacing.xl 的四面间距 */}
      <Text>一些其他内容</Text>

      {/* 右、左外边距为负 – -1 * theme.spacing.xl */}
      <Card.Section>中间部分</Card.Section>

      {/* 下、右、左外边距为负 – -1 * theme.spacing.xl */}
      <Card.Section>最后部分</Card.Section>
    </Card>
  );
}
```

注意，`Card` 依赖直接子元素的映射，你不能为 `Card.Section` 使用片段或其他包装器：

```tsx
import { Card } from '@react-ui/ui';

function Demo() {
  return (
    <Card padding="xl">
      <div>
        <Card.Section>Won't work</Card.Section>
      </div>

      <>
        <Card.Section>Won't work either</Card.Section>
      </>

      <Card.Section>正常运行</Card.Section>
    </Card>
  );
}
```

## 多态 Card.Section

`Card.Section` 是一个[多态组件](/guides/polymorphic/)，你可以更改其根元素：

<Demo data={CardDemos.linkSection} />

## withBorder 和 inheritPadding 属性

- `withBorder` 属性根据 `Card.Section` 相对于其他内容和区域的位置为其添加顶部和底部边框
- `inheritPadding` 属性为 `Card.Section` 添加与 `Card` 组件相同的左右内边距

<Demo data={CardDemos.section} />
