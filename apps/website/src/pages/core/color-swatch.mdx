import { ColorSwatchDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.ColorSwatch);

## 用法

<Demo data={ColorSwatchDemos.usage} />

## withShadow

默认情况下，`ColorSwatch` 具有内部 box-shadow，以使其在浅色背景上更加醒目。
你可以通过设置 `withShadow={false}` 属性来禁用它：

<Demo data={ColorSwatchDemos.shadow} />

<Polymorphic
  defaultElement="div"
  changeToElement="button"
  component="ColorSwatch"
/>

`ColorSwatch` 作为按钮的示例：

<Demo data={ColorSwatchDemos.component} />
