import { ComboboxPopoverDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.ComboboxPopover);

## 用法

`ComboboxPopover` 允许为任何按钮元素添加带有可选选项的 combobox 下拉框。
与 `Select` 和 `MultiSelect` 不同，它不会渲染输入框——而是通过 `ComboboxPopover.Target`
提供你自己的目标元素（通常是 `Button`）。

`ComboboxPopover.Target` 的子元素必须是单个元素或接受 `ref` 的组件。
不支持片段、字符串和其他原始值。

<Demo data={ComboboxPopoverDemos.usage} />

## 数据格式

`ComboboxPopover` 支持与 [Select](/core/select) 组件相同的 `data` 格式：
字符串数组、带有 `value` 和 `label` 的对象，或分组项。

<ComboboxData component="ComboboxPopover" />

## 可搜索

设置 `searchable` 属性以启用下拉框内的搜索输入，在你输入时过滤选项。
使用 `nothingFoundMessage` 在没有选项匹配搜索查询时显示消息。

<Demo data={ComboboxPopoverDemos.searchable} />

## 受控搜索值

使用 `searchValue` 和 `onSearchChange` 属性控制搜索输入值：

<Demo data={ComboboxPopoverDemos.controlledSearch} />

## 排序选项

将 `filter` 属性与 `searchable` 一起使用以提供自定义过滤和排序函数：

<Demo data={ComboboxPopoverDemos.sort} />

## 限制选项

将 `limit` 属性与 `searchable` 一起使用以限制一次显示的选项数量。
这对于提高大型数据集的性能非常有用：

<Demo data={ComboboxPopoverDemos.limit} />

## 多选

设置 `multiple` 属性以允许选择多个值。设置 `multiple` 后，
`value` 类型从 `string | null` 变为 `string[]`，`onChange` 回调
接收一个选中值数组。

<Demo data={ComboboxPopoverDemos.multiple} />

## 勾选图标

<Demo data={ComboboxPopoverDemos.checkIcon} />

## 允许取消选择

默认情况下，可以通过再次点击已选选项来取消选择。
设置 `allowDeselect={false}` 以阻止此行为。

<Demo data={ComboboxPopoverDemos.allowDeselect} />

## 无结果消息

设置 `nothingFoundMessage` 属性以在没有可用选项时显示消息：

<Demo data={ComboboxPopoverDemos.nothingFound} />

## 禁用选项

<Demo data={ComboboxPopoverDemos.disabledOptions} />

## 分组

<Demo data={ComboboxPopoverDemos.groups} />

## 渲染选项

使用 `renderOption` 属性自定义选项渲染：

<Demo data={ComboboxPopoverDemos.renderOption} />

## 大型数据集

默认情况下，下拉框使用 `ScrollArea.Autosize` 包裹。
使用 `maxDropdownHeight` 控制最大高度：

<Demo data={ComboboxPopoverDemos.scrollArea} />

## 控制下拉框打开状态

使用 `dropdownOpened` 属性控制下拉框状态。
此外，你可以使用 `onDropdownOpen` 和 `onDropdownClose` 回调
来响应下拉框状态变化。

<Demo data={ComboboxPopoverDemos.dropdownOpened} />

## 下拉框位置

<Demo data={ComboboxPopoverDemos.dropdownPosition} />

## 下拉框宽度

默认情况下，下拉框宽度与目标元素匹配。使用 `comboboxProps`
设置自定义宽度：

<Demo data={ComboboxPopoverDemos.dropdownWidth} />

## 下拉框动画

<Demo data={ComboboxPopoverDemos.dropdownAnimation} />

## 表单提交

`ComboboxPopover` 渲染一个隐藏 input，其中包含选中的值以进行原生表单提交。
使用 `name` 属性设置 input 名称。对于多选，默认情况下值以逗号连接——
使用 `hiddenInputValuesDivider` 更改分隔符。

<Demo data={ComboboxPopoverDemos.formSubmission} />
