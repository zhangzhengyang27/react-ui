import { DataListDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.DataList);

## 用法

`DataList` 组件渲染一个描述列表（`dl`）元素，包含标签-值对。
它使用语义化的 `dt` 和 `dd` HTML 元素。

<Demo data={DataListDemos.usage} />

## 使用分隔线

设置 `withDivider` 属性以在项之间添加边框：

<Demo data={DataListDemos.divider} />

## 垂直方向

设置 `orientation="vertical"` 以在每个项内将标签堆叠在值上方：

<Demo data={DataListDemos.vertical} />
