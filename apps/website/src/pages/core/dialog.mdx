import { DialogDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Dialog);

## 用法

`Dialog` 是 [Modal](/core/modal/) 组件的简化版本。
它不包含 [Modal](/core/modal/) 的大多数可访问性和可用性功能：

- 焦点陷阱不可用
- 点击外部不会关闭
- 没有遮罩层

使用 `Dialog` 来吸引用户对不重要信息或操作的注意。
例如，你可以创建一个邮件订阅表单：

<Demo data={DialogDemos.usage} />

## 修改位置

`Dialog` 在 [Portal](/core/portal/) 中渲染，并具有固定定位。设置 `position` 属性以控制 dialog 的位置：

```tsx
import { Dialog } from '@react-ui/ui';

function Demo() {
  return (
    <>
      <Dialog position={{ top: 20, left: 20 }} opened>
        Dialog in top left corner
      </Dialog>
      <Dialog position={{ bottom: 20, left: 20 }} opened>
        Dialog in bottom left corner
      </Dialog>
    </>
  );
}
```

## 可访问性

`Dialog` 不具备可访问性，屏幕阅读器很可能不会播报它。
请确保不要在其中放置任何重要信息。在大多数情况下，
选择 [Modal](/core/modal/)、[Drawer](/core/drawer/) 或 [Notifications](/x/notifications/) 会更好。
