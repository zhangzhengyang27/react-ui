import { DrawerDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Drawer);

## 用法

<Demo data={DrawerDemos.usage} />

## 位置

Drawer 可以放置在 `left`（默认）、`top`、`right` 和 `bottom`。使用 `position` 属性控制 drawer 位置，
例如 `<Drawer position="top" />`。

<Demo data={DrawerDemos.positions} />

## 偏移

设置 `offset` 属性以更改 drawer 与视口边缘的偏移：

<Demo data={DrawerDemos.offset} />

## 自定义遮罩层

`Drawer` 使用 [Overlay](/core/overlay/) 组件。你可以使用 `overlayProps` 设置 [Overlay](/core/overlay/) 支持的任何属性：

<Demo data={DrawerDemos.overlay} />

## 尺寸

你可以通过将 `size` 属性设置为预定义尺寸或任何有效宽度来更改 drawer 的宽度/高度（取决于 `position`），
例如 `size="55%"` 或 `size={200}`：

```tsx
import { Drawer } from '@react-ui/ui';

function Demo() {
  return (
    <Drawer position="right" size="xl" opened onClose={() => {}}>
      {/* Drawer content */}
    </Drawer>
  );
}
```

<Demo data={DrawerDemos.sizes} />

## 移除 header

要移除 header，请设置 `withCloseButton={false}`

<Demo data={DrawerDemos.header} />

## 带滚动的 Drawer

<Demo data={DrawerDemos.overflow} />

## 与 ScrollArea 一起使用

<Demo data={DrawerDemos.scrollarea} />

## 修改过渡

`Drawer` 基于 [Transition](/core/transition/) 组件构建。使用 `transitionProps`
属性自定义任何 [Transition](/core/transition/) 属性：

<Demo data={DrawerDemos.transitions} />

## onExitTransitionEnd 和 onEnterTransitionEnd

`onExitTransitionEnd` 和 `onEnterTransitionEnd` 属性可用于在
退出/进入过渡完成后运行代码。例如，当你想在 drawer 关闭后清除数据时非常有用：

<Demo data={DrawerDemos.transitionEnd} />

## 初始焦点

`Drawer` 使用 [FocusTrap](/core/focus-trap/) 来捕获焦点。将 `data-autofocus`
属性添加到应接收初始焦点的元素。

<Demo data={DrawerDemos.initialFocus} />

如果你不想在 drawer 打开时聚焦任何元素，请使用 `FocusTrap.InitialFocus`
组件创建一个视觉上隐藏的元素来接收初始焦点：

<Demo data={DrawerDemos.initialFocusTrap} />

如果你没有添加 `data-autofocus` 属性且没有使用 `FocusTrap.InitialFocus`，
drawer 将聚焦其内部的第一个可聚焦元素，通常是关闭按钮。

## 控制行为

以下属性可用于控制 `Drawer` 行为。
在大多数情况下，不建议关闭这些功能——
这会使组件的可访问性降低。

- `trapFocus` – 确定焦点是否应被捕获在 drawer 内部
- `closeOnEscape` – 确定按下 `Escape` 键时是否应关闭 drawer
- `closeOnClickOutside` – 确定用户点击遮罩层时是否应关闭 drawer
- `returnFocus` – 确定是否应将焦点返回到打开 drawer 之前聚焦的元素

## react-remove-scroll 设置

`Drawer` 使用 [react-remove-scroll](https://github.com/theKashey/react-remove-scroll)
包来锁定滚动。你可以通过 `removeScrollProps` 将属性传递给 `RemoveScroll` 组件：

```tsx
import { Drawer } from '@react-ui/ui';

function Demo() {
  return (
    <Drawer
      removeScrollProps={{ allowPinchZoom: true }}
      opened
      onClose={() => {}}
    >
      {/* Drawer content */}
    </Drawer>
  );
}
```

## 修改关闭图标

使用 `closeButtonProps` 自定义关闭按钮：

<Demo data={DrawerDemos.closeIcon} />

## 复合组件

你可以使用以下复合组件来完全控制 `Drawer` 的渲染：

- `Drawer.Root` – 上下文提供者
- `Drawer.Overlay` – 渲染 [Overlay](/core/overlay/)
- `Drawer.Content` – 主要 drawer 元素，应包含所有 drawer 内容
- `Drawer.Header` – 粘性 header，通常包含 `Drawer.Title` 和 `Drawer.CloseButton`
- `Drawer.Title` – `h2` 元素，`Drawer.Content` 的 `aria-labelledby` 指向此元素，通常渲染在 `Drawer.Header` 内
- `Drawer.CloseButton` – 关闭按钮，通常渲染在 `Drawer.Header` 内
- `Drawer.Body` – 主要内容区域，`Drawer.Content` 的 `aria-describedby` 指向此元素

<Demo data={DrawerDemos.composition} />

## Drawer.Stack

使用 `Drawer.Stack` 组件同时渲染多个 drawer。
`Drawer.Stack` 跟踪打开的 drawer，管理 z-index 值、焦点捕获
和 `closeOnEscape` 行为。`Drawer.Stack` 设计为与 `useDrawersStack` Hook 一起使用。

与使用多个 `Drawer` 组件的区别：

- `Drawer.Stack` 管理 z-index 值——后打开的 drawer 始终具有更高的 z-index 值，无论它们在 DOM 中的顺序如何
- `Drawer.Stack` 禁用焦点捕获和除当前打开的 drawer 之外所有 drawer 的 `Escape` 键处理
- 当前未打开的 drawer 存在于 DOM 中，但使用 `opacity: 0` 和 `pointer-events: none` 隐藏
- 一次只渲染一个遮罩层

<Demo data={DrawerDemos.stack} />

请注意，`Drawer.Stack` 只能与 `Drawer` 组件一起使用。使用 `Drawer.Root`
和其他复合组件构建的组件与 `Drawer.Stack` 不兼容。

## useDrawersStack hook

`useDrawersStack` Hook 提供了一种同时控制多个 drawer 的简便方法。
它接受一个唯一的 drawer id 数组，并返回一个具有以下属性的对象：

```tsx
interface UseDrawersStackReturnType<T extends string> {
  // 每个 drawer 的当前打开状态
  state: Record<T, boolean>;

  // 打开给定 id 的 drawer
  open: (id: T) => void;

  // 关闭给定 id 的 drawer
  close: (id: T) => void;

  // 切换给定 id 的 drawer
  toggle: (id: T) => void;

  // 关闭堆栈中的所有 drawer
  closeAll: () => void;

  // 返回给定 id drawer 的属性
  register: (id: T) => {
    opened: boolean;
    onClose: () => void;
    stackId: T;
  };
}
```

将 `useDrawersStack` 与 `Drawer` 组件一起使用的示例：

```tsx
import { Drawer, useDrawersStack } from '@react-ui/ui';

function Demo() {
  const stack = useDrawersStack(['first', 'second']);

  return (
    <>
      <Drawer {...stack.register('first')}>第一</Drawer>
      <Drawer {...stack.register('second')}>第二</Drawer>
      <Button onClick={() => stack.open('first')}>打开第一个</Button>
    </>
  );
}
```

## 固定元素偏移

`Drawer` 组件使用 [react-remove-scroll](https://github.com/theKashey/react-remove-scroll)
包来锁定滚动。要正确调整这些 `elements` 的大小，请向它们添加 `className`（[文档](https://github.com/theKashey/react-remove-scroll#positionfixed-elements)）：

```tsx
import { RemoveScroll } from '@react-ui/ui';

function Demo() {
  return (
    <>
      <div className={RemoveScroll.classNames.fullWidth}>
        width: 100%
      </div>
      <div className={RemoveScroll.classNames.zeroRight}>
        right: 0
      </div>
    </>
  );
}
```

## 可访问性

`Drawer` 组件遵循 [WAI-ARIA 建议](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/dialog) 实现可访问性。

设置 `title` 属性以使组件可访问，它将为内容元素添加 `aria-labelledby`：

```tsx
import { Drawer } from '@react-ui/ui';

function Demo() {
  return <Drawer title="抽屉标签" opened onClose={() => {}} />;
}
```

要使用关闭按钮 `aria-label`，请使用 `closeButtonProps`：

```tsx
import { Drawer } from '@react-ui/ui';

function Demo() {
  return (
    <Drawer
      closeButtonProps={{ 'aria-label': '关闭抽屉' }}
      opened
      onClose={() => {}}
    />
  );
}
```
