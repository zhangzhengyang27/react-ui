import { ColorPickerDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.HueSlider);

## 用法

使用 `HueSlider` 组件允许用户选择颜色的色相值。
它接受 `0` 到 `360` 之间的值。`HueSlider` 是 [ColorPicker](/core/color-picker) 组件的一部分，
但也可以单独使用。

<Demo data={ColorPickerDemos.hueSlider} />
