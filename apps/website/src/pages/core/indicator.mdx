import { IndicatorDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Indicator);

## 用法

<Demo data={IndicatorDemos.configurator} />

## 行内模式

当目标元素具有固定宽度时，设置 `inline` 属性为 Indicator 容器添加 `display: inline-block;` 样式。
或者，如果你仍希望根元素保持 `display: block`，可以使用 `style` 属性设置宽度和高度。

<Demo data={IndicatorDemos.inline} />

## 偏移

设置 `offset` 以更改指示器位置。当 Indicator 组件与具有 border-radius 的子元素一起使用时，这很有用。
你可以传入一个数字实现统一偏移，或传入包含 `x` 和 `y` 属性的对象分别设置水平和垂直偏移：

<Demo data={IndicatorDemos.offset} />

## 处理中动画

<Demo data={IndicatorDemos.processing} />

## 禁用

设置 `disabled` 以隐藏指示器：

<Demo data={IndicatorDemos.disabled} />

## 最大值

设置 `maxValue` 属性，当标签超过最大值时显示 `{maxValue}+`。这对于不应显示精确大数字的通知计数器很有用：

<Demo data={IndicatorDemos.maxValue} />

## 显示零

默认情况下，当标签为 `0` 时指示器会显示。设置 `showZero={false}` 以在标签为 `0` 时隐藏指示器：

<Demo data={IndicatorDemos.showZero} />

## 自动对比度

设置 `autoContrast` 属性以根据背景色自动调整文本颜色，确保可读对比度：

<Demo data={IndicatorDemos.autoContrast} />
