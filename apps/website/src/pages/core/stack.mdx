import { StackDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Stack);

## 用法

`Stack` 是一个垂直 flex 容器。如果你需要水平 flex 容器，请改用 [Group](/core/group)
组件。如果你需要完全控制 flex 容器属性，请使用 [Flex](/core/flex) 组件。

<Demo data={StackDemos.configurator} />

<FlexboxGapSupport component="Stack" />
