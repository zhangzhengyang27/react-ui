import { TitleDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Title);

## 用法

使用 Title 组件渲染带有 ReactUI [theme](/theming/theme-object) 样式的 h1-h6 标题。
默认情况下，`Title` 没有外边距和内边距。
你可以通过 [theme.headings](/theming/typography) 为每个标题更改 `font-size`、`font-weight` 和 `line-height`。

设置 `order` 属性以渲染特定元素（h1-h6）；默认 order 为 `1`：

<Demo data={TitleDemos.usage} />

## 尺寸

你可以独立于 `order` 更改 Title 的 `size`：

- 如果将 size 设置为 `h1`-`h6`，则组件会从 [theme](/theming/theme-object/) 添加相应的 `font-size` 和 `line-height`
- 如果将 size 设置为任何其他值，则 `line-height` 将基于 `order` 计算 – `size` 只影响 `font-size`

<Demo data={TitleDemos.size} />

## 文本换行

使用 `textWrap` 属性控制 [text-wrap](https://developer.mozilla.org/en-US/docs/Web/CSS/text-wrap)
CSS 属性。它控制元素内部文本的换行方式。

<Demo data={TitleDemos.textWrap} />

你也可以在 [theme](/theming/theme-object) 上设置 `textWrap`：

```tsx
import { createTheme, UIProvider, Title } from '@react-ui/ui';

const theme = createTheme({
  headings: {
    textWrap: 'wrap',
  },
});

function Demo() {
  return (
    <UIProvider theme={theme}>
      <Title>某个应该换行的很长标题</Title>
    </UIProvider>
  );
}
```

## 行数限制

设置 `lineClamp` 属性以在指定行数后截断文本：

<Demo data={TitleDemos.lineClamp} />
