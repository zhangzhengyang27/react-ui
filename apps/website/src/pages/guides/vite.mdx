import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Vite);

# 在 Vite 中使用

<GetTemplates type="vite" />

## 创建新应用

按照 [Vite 入门指南](https://vitejs.dev/guide/) 创建新的 Vite 应用：

<NpmScript
  yarnScript="yarn create vite"
  npmScript="npm create vite@latest"
/>

## 安装

<PackagesInstallation />

## PostCSS 配置

安装 PostCSS 插件和 [postcss-preset-ui](/styles/postcss-preset)：

<InstallScript
  packages="postcss postcss-preset-ui postcss-simple-vars"
  dev
/>

在应用根目录创建 `postcss.config.cjs` 文件，内容如下：

```js
module.exports = {
  plugins: {
    'postcss-preset-ui': {},
    'postcss-simple-vars': {
      variables: {
        'ui-breakpoint-xs': '36em',
        'ui-breakpoint-sm': '48em',
        'ui-breakpoint-md': '62em',
        'ui-breakpoint-lg': '75em',
        'ui-breakpoint-xl': '88em',
      },
    },
  },
};
```

## 配置

在应用根组件（通常是 `App.tsx`）中添加样式导入和 [UIProvider](/theming/ui-provider)：

```tsx
// 导入你已安装包的样式。
// 除 `@react-ui/hooks` 外，所有包都需要导入样式
import '@react-ui/ui/styles.css';

import { UIProvider } from '@react-ui/ui';

export default function App() {
  return <UIProvider>{/* 你的应用内容 */}</UIProvider>;
}
```

配置完成！启动开发服务器：

```bash
npm run dev
```
