import { UseForceUpdateDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.useForceUpdate);

## 用法

`use-force-update` Hook 返回一个函数，调用它会重新渲染组件：

<Demo data={UseForceUpdateDemos.usage} />

## 类型定义

```tsx
function useForceUpdate(): () => void;
```
