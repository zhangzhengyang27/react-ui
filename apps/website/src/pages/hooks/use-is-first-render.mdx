import { UseIsFirstRenderDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.useIsFirstRender);

## 用法

`useIsFirstRender` Hook 在组件首次渲染时返回 `true`；否则返回 `false`。

<Demo data={UseIsFirstRenderDemos.usage} />

## 类型定义

```tsx
function useIsFirstRender(): boolean;
```
