import { UsePageLeaveDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.usePageLeave);

## 用法

`use-page-leave` Hook 在鼠标离开页面时调用给定函数：

<Demo data={UsePageLeaveDemos.usage} />

## 类型定义

```tsx
function usePageLeave(onPageLeave: () => void): void;
```
