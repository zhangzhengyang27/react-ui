import { CssVariablesList } from '@/components/CssVariablesList';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.CSSVariablesList);

# 默认 CSS 变量列表

本页列出了所有由默认主题生成的 ReactUI CSS 变量。

<CssVariablesList />
