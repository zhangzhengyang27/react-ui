import { NprogressDemos } from '@react-ui/docs-demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Nprogress);

## Installation

<InstallScript packages="@react-ui/ui" />

After installation import package styles at the root of your application:

```tsx
import '@react-ui/ui/styles.css';
// ‼️ import nprogress styles after core package styles
import '@react-ui/ui/styles.css';
```

## Setup NavigationProgress

Render the `NavigationProgress` component anywhere in your app within [MantineProvider](/theming/mantine-provider/):

```tsx
import { MantineProvider } from '@react-ui/ui';
import { NavigationProgress } from '@react-ui/ui';

function Demo() {
  return (
    <MantineProvider>
      <NavigationProgress />
      {/* Your app here */}
    </MantineProvider>
  );
}
```

## Usage

<Demo data={NprogressDemos.usage} />
