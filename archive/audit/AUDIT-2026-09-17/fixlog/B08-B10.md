# B08 / B09 / B10 修复日志

- 修复日期:2026-09-17
- 范围:packages/ui/src/components/{ActionIcon,Button,CloseButton,UnstyledButton,Alert,Notification,Notifications,Carousel,CodeHighlight,ColorSwatch,CopyButton,Dropzone,Loader,NavigationProgress,Pill,TableOfContents,Transition,FocusTrap,Portal,TypographyStylesProvider}/** 与 packages/ui/src/dates/**
- 状态图例:✅已修 | ⏭️跳过(注明原因) | 🔍核实后处理

## B08(散件补漏)

| 单号 | 状态 | 改动文件与要点 | 定向测试 |
|---|---|---|---|
| B08-1 | ✅ | CloseButton.module.css:两处暗色选择器改为祖先形式 `[data-ui-color-scheme='dark'] &`(对齐 Paper/Card 修法),暗色 color 与 subtle hover 背景恢复生效 | components 定向套件全绿 |
| B08-2 | ✅ | NotificationContainer.tsx:新增 `className/style` props,接收 Notifications `getStyles('notification')` 产物并与通知自身 className/style 合并(cx / spread)后传给 `<Notification>`,`notification` 样式槽接线生效 | Notifications.test 4/4 |
| B08-3 | ✅ | Notification.tsx:varsResolver 改用 `getThemeColor(resolvedColor, theme)`——主题键走 `var(--ui-color-*-filled)`,任意 CSS 颜色(#ff0000 等)原样透传,不再拼非法变量名 | Notification 套件全绿 |
| B08-4 | ✅ | Button.tsx / ActionIcon.tsx:根元素补 `aria-busy={loading || undefined}` | Button/ActionIcon 全绿 |
| B08-5 | ✅ | UnstyledButton.tsx:解构 `disabled/onClick`,非原生 button 组件(component!=='button')时不透传 `disabled` 属性,改为 onClick 内 `preventDefault + return` 拦截并加 `aria-disabled`;`disabled` 属性仍按原样透传给原生 button。Button/ActionIcon 经 UnstyledButton 统一生效 | Button/ActionIcon/Carousel(依赖 UnstyledButton)全绿 |
| B08-6 | ✅ | Button.module.css:hover 包 `@media (hover: hover)` 并补 `(hover: none)` active 分支;CloseButton.module.css subtle hover 同样包媒体守卫,对齐 ActionIcon 写法 | 全绿 |
| B08-7 | ⏭️ | 默认 aria-label 中文化跳过:现有测试编码了英文默认文案(Carousel.test.tsx:58-81 断言 'Previous slide'/'Next slide'/tablist 'Slides';Notifications.test.tsx:35 断言 /Close notification/i),按「以测试为准」不修改 | 原测试保持通过 |
| B08-8 | ✅ | Notifications.tsx:limit/position 变化的 effect 中调用 `notifications.updateState(store, list => list)` 立即重排一次,已显示列表即时收敛;顺带移除死代码 `?? 5` | Notifications.test 4/4 |
| B08-9 | ✅ | NotificationContainer.tsx:解构剔除 `id/position/priority/__sequence`,不再随 spread 泄漏为 DOM 属性(id 仍经 data.id 使用) | Notifications.test 4/4 |
| B08-10 | ✅ | Carousel.tsx:`onKeyDownCapture` 改为冒泡阶段 `onKeyDown`,并增加 `event.defaultPrevented` 早退守卫 | Carousel.test 全绿(键盘用例仍过) |
| B08-11 | ✅(部分) | Carousel.tsx:region 补 `aria-label="carousel"`(aria-roledescription 要求可访问名称,消费者可经 aria-label 覆盖)。指示器退回普通按钮+aria-current 的方案与测试编码的 role="tablist"/tab 冲突,维持 tab 语义 | Carousel.test 全绿 |
| B08-12 | ✅ | Pill.tsx:移除按钮在 PillsInput 上下文内保持 aria-hidden+tabIndex=-1;独立使用(无上下文)时改为 `tabIndex={0}`、取消 aria-hidden 并补 `aria-label="移除"`(consumer 可经 removeButtonProps 覆盖) | Pill 无独立测试,组件套件全绿 |
| B08-13 | ✅ | Loader.tsx:`loaders[type]` 未注册时回落 `defaultLoaders.oval` 并 dev console.warn | Loader 套件全绿 |
| B08-14 | ✅ | Loader.tsx:`LoaderProps` 基类改 `ElementProps<'div','display'\|'opacity'>`,Factory ref 改 `HTMLDivElement`,与实际渲染 span/div 对齐 | 全绿 + tsc 无新增错误 |
| B08-15 | ✅ | Upload.tsx:maxFiles 槽位改在 commit 前按最新 `filesRef.current.length` 收口(`Math.max(maxFiles, currentFiles.length)` 为上限裁剪,已超额时只挡新文件不挤已有文件),autoUpload 只上传实际提交的文件;顺带补 `FileWithPath` 类型导入(修复既有 TS2304) | Upload.test 全绿 |
| B08-16 | ✅ | Upload.tsx:错误文案从 fileSize 的 Text 拆出独立节点 `getStyles('fileError')` 渲染,走红色错误样式槽;顺带修复 fileInfo 处 `style` 双写覆盖(TS2783)改为经 `getStyles('fileInfo',{style})` 合并 | Upload.test 全绿 |
| B08-17 | ✅ | Portal.tsx:新增 `pickPortalNodeAttrs`,data-*/aria-* 与 className/style/id 一并同步到自建容器节点(sync 时先清后写防残留,保留 data-portal 等保留标记) | Portal.test 4/4 |
| B08-18 | ✅ | ActionIconGroup.tsx:删除重复的第二次 `useProps` 调用,直接解构第一次结果 | ActionIcon 全绿 |
| B08-19 | ✅ | ActionIconGroupSection.tsx:解构列表补 `color`,不再渲染 `<div color="blue">` | ActionIcon 全绿 |
| B08-20 | ✅ | CodeHighlightTabs.tsx:文件按钮 key 改 `node.fileName ?? index` | CodeHighlightTabs.test 20/20 |
| B08-21 | ✅ | CodeHighlightTabs.tsx:文件条容器补 `role="tablist"`,文件按钮补 `role="tab"` + `aria-selected` | CodeHighlightTabs.test 20/20 |
| B08-22 | ✅ | CodeHighlight.tsx:四处 `@default` 注释修正为实际中文默认值('复制'/'已复制'/'展开代码'/'收起代码');Pill.tsx:radius 注释移除 `@default 'xl'`,标注 CSS 兜底 `--pill-radius, 1000rem` | 全绿 |
| B08-23 | ✅ | Notification.module.css:色条圆角改 `border-start/end-start-radius`;TableOfContents.module.css:`padding-left` → `padding-inline-start`;TypographyStylesProvider.module.css:ul/ol `padding-inline-start`、blockquote `border-inline-start` | 全绿 |
| B08-T1 | ✅(核实属实,已修) | 核实:库内浮层默认不向 Portal 传 className/style(PopoverDropdown/ModalBase/Notifications 均未传),但 ModalBase 经 `portalProps` 逃生口可达;机制上共享节点整体覆盖属性且卸载不恢复属实。按⑦批方案扩展:Portal.tsx 带属性实例(`hasNodeAttrs`)强制使用独立容器节点(`shouldReuseNode = reuseTargetNode && !hasNodeAttrs`),独立节点随实例创建/移除,无属性实例继续共享节点,从根上消除互相覆盖与不可恢复问题 | Portal.test 4/4 |

## B09(dates components)

| 单号 | 状态 | 改动文件与要点 | 定向测试 |
|---|---|---|---|
| B09-1 | ✅ | get-time-string.ts:hours/minutes/seconds 增加 `Number.isFinite` 校验(NaN 不再产出假合法 'NaN:NaN');clamp-time.ts:非法 time 原样返回、非法 min/max 按缺省处理,onPaste 链路非法输入不上报不清空已有值。补 2 条测试(整串/半串非法粘贴,断言 onChange 不触发且可用值仍可粘贴) | TimePicker.test 77/77(含新增) |
| B09-2 | ✅ | clamp-time.ts:增加 `withSeconds = true` 参数,输出格式随之(HH:mm / HH:mm:ss);TimePicker.tsx handleBlur 与 use-time-picker.ts onPaste 调用点传入 `withSeconds`。补 1 条测试(withSeconds=false 失焦钳制上报 '10:30' 而非 '10:30:00') | TimePicker.test 77/77 |
| B09-3 | ✅ | Calendar.tsx:6 个导航 handler(handleNext/Previous Month/Year/Decade)全部 useCallback 化,依赖 [currentDate, _columnsToScroll, onXxx, setDate],document keydown effect 不再逐渲染重挂 | dates 全量 1438/1438 |
| B09-4 | ✅ | DatePicker.tsx:显式解构 `type`,`useDatesState` 直接使用,不再经 rest/others 泄漏为日历根 DOM 属性 | dates 全量全绿 |
| B09-5 | ✅ | InlineDateTimePicker.tsx:currentLevel 初值与受控 level 同步(useDidUpdate)均经 `clampLevel(level \|\| defaultLevel \|\| 'month', calendarProps.minLevel, calendarProps.maxLevel)`,与 Calendar 实际显示层级一致,时间面板不再被错误隐藏(maxLevel="month"+defaultLevel="year" 场景) | InlineDateTimePicker.test 28/28 |
| B09-6 | ✅ | InlineDateTimePicker.tsx:开始/结束时间输入清空(空串)时,同步把合成值的时间部分剥离(stripTime → 'YYYY-MM-DD'),显示与提交值一致,旧时间不再复活 | InlineDateTimePicker.test 28/28 |
| B09-7 | ✅ | TimeControlsList.tsx:getValuesRange 入口 `step = Math.max(1, Math.floor(step))`,step<=0 不再渲染期死循环;get-time-range.ts:interval<=0/非法返回 [startTime],起止非法返回 [] | TimePicker 套件全绿 |
| B09-8 | ✅ | AmPmInput.tsx:input 模式 onChange 归一化——空串→null;首字符 a/p 映射 labels.am/labels.pm;不匹配时忽略输入保持旧值,am/pm 语义不再静默失效 | TimePicker.test 全绿 |
| B09-9 | ✅ | DateInput.tsx:handleInputChange 清空分支改用归一后的 `_allowDeselect`(= allowDeselect ?? clearable),与点选路径一致 | dates 全量全绿 |
| B09-10 | ✅ | InlineDateTimePicker.tsx:handleDefaultDateChange 处理 null 分支(allowDeselect 反选)→ `setValue(null)`,时间状态随 useDidUpdate [_value] 同步清空 | InlineDateTimePicker.test 28/28 |
| B09-11 | ✅ | DatePicker.tsx:预设选择后 `setLevelRef.current?.(clampLevel('month', calendarProps.minLevel, calendarProps.maxLevel))`,不再绕过 minLevel(补 clampLevel 导入) | dates 全量全绿 |
| B09-12 | ✅ | PickerInputBase.tsx:Modal onClose 改走统一关闭管线——先 `handleClose()`(清理未完成区间+关闭)再 `onDropdownClose?.()`(消费侧钳制+回调),与 popover 路径行为一致 | DateTimePicker.test 72/72 |
| B09-13 | ✅ | TimeControl.tsx:恢复 `tabIndex={0}`(原 -1 使键盘完全不可达;onMouseDown preventDefault 保持输入框焦点不受影响)。TimePresetControl 经核实际未设 tabIndex(UnstyledButton 默认可聚焦),无需修改,维持原状 | TimePicker.test 全绿 |

## B10(dates hooks/utils)

| 单号 | 状态 | 改动文件与要点 | 定向测试 |
|---|---|---|---|
| B10-1 | ✅ | use-uncontrolled-dates.ts:补 `type === 'default'` 切回分支(数组取首个非 null ?? null);multiple 分支改为滤 null(`filter(v => v != null)`);range 分支维持 [start,end] 补齐。补 2 条测试(range→default 取首个、range 半区间→multiple 滤 null) | use-uncontrolled-dates.test 全绿(含新增) |
| B10-2 | ✅ | use-uncontrolled-dates.ts:渲染期不再调用 `_setValue`——切 type 派生值挂 `pendingValueRef`,提交移入 effect(setter 的 onChange 转发不再发生在渲染期,消除 React "Cannot update a component while rendering" 与 StrictMode 双触发风险;渲染期返回值仍是正确形状,UI 无闪断)。补 1 条测试(切 type 后 onChange 恰好一次、值与派生结果一致) | use-uncontrolled-dates.test 全绿 |
| B10-3 | ✅ | get-formatted-date.ts:default 分支空串与 null 同视为空值(`date ? ... : ''`),multiple 分支滤空串元素——受控 `value=""` 不再显示 "Invalid Date";use-dates-input.ts:shouldClear 改 `!!_value`,空串不误判为有值(toDateString 保持原语义,在上层消费点兜底) | dates 全量全绿 |
| B10-4 | ✅ | assign-time.ts:分量钳制——hours>23 对齐 schedule 包 toTimeString 语义(23:59:x,不跨天),minutes/seconds 钳 0-59,NaN/Infinity fallback 0,不再产出 Invalid Date('24:00' → 当日 23:59:00) | dates 全量全绿 |

## 测试汇总

- B08 组件定向套件:`src/components/{Button,ActionIcon,Alert,Notification,Notifications,Carousel,CodeHighlight,CopyButton,Dropzone,Loader,Pill,TableOfContents,Portal,CloseButton,UnstyledButton}` → 全部通过(最后回归 89/89,B08 全量套件此前 161/161 + Portal/Notifications 8/8)。
- B09/B10 dates 全量:`npx vitest run src/dates` → 69 文件 / 1438 用例全部通过。
- 新增测试:5 条(TimePicker 2 条:B09-1 非法粘贴、B09-2 钳制格式;use-uncontrolled-dates 3 条:B10-1 切回 default、multiple 滤 null、B10-2 onChange 提交时机)。
- 类型检查:`tsc --noEmit` 全库错误数 215 → 210,未新增错误(修复了 Upload.tsx 的 TS2304/TS2783;其余均为本批范围外的既有错误,如 AgendaView Factory 导入、隐式 any 等)。
- 跳过项:B08-7(默认 aria-label 中文化)——现有测试编码英文默认文案,按「以测试为准」跳过;B09-13 的 TimePresetControl 部分——核实其本就未设 tabIndex,无需修改。
