# B11 / B12 修复日志(schedule 视图 + schedule hooks/utils)

修复日期:2026-09-17。范围:`packages/ui/src/schedule/**` 与被 B11-1 波及的 `Factory` 断链(采用 AgendaView 内 re-export 方案,dates 29 处导入行零改动)。

## tsc 基线对比

| 时点 | ui 包 `tsc --noEmit` 错误总数 | 说明 |
|---|---|---|
| 修复前基线 | 210 | 其中 TS2459 × 47、TS2303 × 1;schedule 相关 99、dates 相关 99 |
| B11-1 后 | 33 | TS2459/TS2303 全清 |
| B11-B12 全部修复后 | 33 | schedule 仅剩 3 条基线既有的潜在旧债(见下) |

- 最终 33 条中 10 条不在基线清单内,均为 Factory 断链期间被级联 any「压制」、修复后重新暴露的**潜在旧债**(dates 组件/story 的 Popover props、`handlers.set` 等),非本次代码引入;另含并行批次在 `form/use-form.ts` 的改动噪音(共享工作树,与本批无关)。
- schedule 残留 3 条均为基线既有潜在债、不在本批问题单内,未动:`MonthYearSelect.tsx(235)` `handlers.set` TS2339(useDisclosure 无 set,接线属行为变更,超出本批范围)、`MonthYearSelect.story.tsx(27/38)` Popover children TS2741(story 类型摩擦)。
- 定向目标达成:TS2459(47)→0、TS2303(1)→0、`use-drag-drop-handlers.ts:230` TS2322(B12-3)→0。

## 定向测试结果

`npx vitest run src/schedule`:**87 文件 / 1344 测试全绿**(基线 1349 − B12-4 删除的 5 条死 API 用例,零失败)。耗时约 7 分钟。
B12-1 另做跨时区定向验证(TZ=Asia/Shanghai / America/New_York / UTC 各 2 用例,验证后已删除临时测试文件):全部通过。

---

## 逐条记录

| 单号 | 状态 | 改动文件与要点 |
|---|---|---|
| B11-1 | 已修 | `AgendaView.tsx`:`import { Factory } from './AgendaView'`(TS2303 自引用)改从规范出处 `core/factory/create-factory` 导入,并 `export type { Factory }` re-export——包合并(f89dac7e)改写的 47 处旧导入路径(18 schedule + 29 dates)零改动即恢复解析,改动面最小、无运行时影响。验证:210→33,TS2459/TS2303 全清 |
| B11-2 | 已修 | `handle-year-view-key-down.ts`:`!controlToFocus` 提前 return 并入「跳过并继续递归」分支(与 disabled/data-hidden/data-outside/data-day-placeholder 同路,对齐 ⑱批 MonthView 方案);`withOutsideDays=false` 时方向键跨占位格不再中断 |
| B11-3 | 已修 | `MobileMonthView.tsx`:事件时间文案 `'All day'` → `getLabel('allDay', labels)`;新增 `weekNumberLabel: (n) => \`Week ${n}\`` label key(`labels.ts` 接口 + 默认值,默认输出与原硬编码一致),三处周序号 `aria-label` 硬编码统一走 getLabel(`MobileMonthView.tsx`、`MonthView.tsx`、`YearViewMonth.tsx`,YearViewMonth 的 `title` 同步)。默认 labels 下测试可见输出不变 |
| B11-4 | 已修 | `MobileMonthView.tsx`:无 role 的 button 上无效的 `aria-selected` 移除,改挂 `aria-current={isSelected ? 'date' : undefined}`(采用审计建议的最小方案,未改造 grid/gridcell 语义)。全 schedule 测试无 aria-selected 断言,零破坏 |
| B11-5 | 已修 | `ViewSelect.tsx`:补 `hasActiveValue` 兜底,无激活值时第一项 `tabIndex=0`;`MonthYearSelect.tsx`:月份列表补 `hasActiveMonth` 兜底(对齐同文件年份列表既有兜底与 ⑩批 Tabs 处理) |
| B11-6 | 已修 | 三视图展开管线套 useMemo(对齐 ②批模式):`MobileMonthView.tsx`(expanded + grouped,deps events/date/recurrenceExpansionLimit);`AgendaView.tsx`(expanded + grouped);`YearView.tsx`(expanded + grouped;另 `monthsList` 与 `firstDayIndexByMonth` 一并 useMemo + `getFirstDayIndex` useCallback,消除每渲染 12× getMonthDays 重算) |
| B12-1 | 已修 | `expand-recurring-events.ts`:`set.between` 的边界改为 `naiveToUTCNaive(...)` 编码(searchStart 含 duration 回看、rangeEnd),与候选值「墙钟分量编码为 UTC」同编码,消除 |UTC offset| 的窗口偏移(东八区丢末日 16:00 后、西五区丢首日 05:00 前的实例)。tzid=本地时恒等映射语义不变(返回值仍经 toISOString 读 UTC 分量还原)。跨三时区定向验证通过;2026-03-08 美东 02:00 缺失为 DST 墙钟不存在的正确语义,非边界缺陷 |
| B12-2 | 已修 | `get-start-of-week.ts`:循环前补 `if (!value.isValid()) return value;`(对齐 `get-end-of-week.ts`),非法输入(空串/null)不再死循环 |
| B12-3 | 已修 | `DragContext.tsx`:`DragContextValue` 泛型化 `<T = unknown>`(dropTarget/setDropTarget 随 T;setDropTarget 用方法双变声明桥接非泛型 context,有注释);`use-drag-drop-handlers.ts`:`dragContextValue: DragContextValue<T>`,本地 state setter 更名 `setDropTargetState`,context 值直接贯通 T,消除 TS2322 与 `as T` 强转。各视图 T(string/{day,slotIndex}/{resourceId,slotIndex} 等)无需改动 |
| B12-4 | 已修 | `use-drag-state.ts`:DropTarget 改为从 `../types` re-export(单一来源,`hooks/index.ts` 导出面不变);删除 `DragState.dropTarget` 字段与 `setDropTarget/clearDropTarget` 死 API(全仓零消费已核实,唯一接线方 use-drag-drop-handlers 读的是 hook 本地 state)。`use-drag-state.test.ts` 同步删除 5 条死 API 用例、状态断言移除 dropTarget 字段 |
| B12-5 | 已修 | `sort-events.ts`:`events.toSorted(...)` → `[...events].sort(...)`(不原地排序语义不变;toSorted 为 ES2023 API,与 target ES2015 不符) |
| B12-6 | 已修 | `to-date-string.ts`:`format('YYYY-MM-DD 00:00:00')` → `format('YYYY-MM-DD')`(对齐 DateStringValue 契约,10 字符)。消费方排查:全部内部消费方仅做 dayjs 再解析/同源 Map key,行为不变;3 处依赖旧行为的消费方已改:`get-week-days.ts`/`get-month-range.ts` 改为自行 format 保持各自 19 字符的测试编码输出不变(不再借 toDateString 的错标输出),`ResourcesMonthView.test.tsx` 3 处 19 字符字面量断言改为 10 字符(与其余视图自引用式断言一致)。`to-date-string.test.ts` 断言更新为 10 字符。注:`src/index.ts:18-21` 关于「schedule 版会带 00:00:00」的注释因不在本批允许范围未同步,现已过时(两版行为一致) |
| B12-7 | 已修 | `use-event-resize.ts` / `use-horizontal-event-resize.ts`:新增 `resizeMathRef`(渲染期更新,持有 snapPercent/percentToDateTime/minHeightPercent 或 minWidthPercent),监听器内经 ref 读最新值;effect 依赖保持 `[isResizing]` 不引起拖拽中重绑(对齐审计建议的 ref 方案) |

跳过:0 条。两条「待核实」按任务书不修(B11 ResourcesMonthView 本地 isMultiDayEvent 语义、B12 RTL 方向语义与 useSlotDragSelect 指针卫生)。

## 备注

- B11-3 的 `weekNumberLabel` 为新增必填 label key(接口 + DEFAULT_SCHEDULE_LABELS 均已补);`ScheduleLabelsOverride` 为 Partial,既有覆盖方不受影响,未传时默认输出与原硬编码逐字节一致。
- B12-6 未波及 `getMonthDays`(其 19 字符输出为自行 format,不走 toDateString,保持不变);视图导航 onDateChange 载荷由 19 字符变为 10 字符,与 DateStringValue 契约及 `ScheduleHeaderBase.test` 既有 10 字符断言一致。
