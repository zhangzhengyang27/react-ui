import { Calendar } from '@react-ui/dates';
import { UIDemo } from '@react-ui/demo';

const code = `
import { Calendar } from '@react-ui/dates';

function Demo() {
  return <Calendar />;
}
`;

function Demo() {
  return <Calendar />;
}

export const usage: UIDemo = {
  type: 'code',
  centered: true,
  component: Demo,
  code,
};
