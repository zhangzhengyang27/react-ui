import type { TypographyFactory } from '@react-ui/ui';
import type { StylesApiData } from '../types';

export const TypographyStylesApi: StylesApiData<TypographyFactory> = {
  selectors: {
    root: 'Root element',
  },

  vars: {},
};
