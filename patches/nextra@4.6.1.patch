diff --git a/dist/server/loader.js b/dist/server/loader.js
index bd749814ff1c5b73bc26cde7bb02fb2af76530dc..62e1dc64597e5f0479922da8ca8efdbf87e8272b 100644
--- a/dist/server/loader.js
+++ b/dist/server/loader.js
@@ -137,18 +137,8 @@ ${locales.map((lang) => `"${lang}": () => import("${importPath}?lang=${lang}")`)
   return rawJs.replace(rawImport, replaced);
 }
 async function getLastCommitTime(filePath) {
-  if (!repository) {
-    return;
-  }
-  const relativePath = path.relative(GIT_ROOT, filePath);
-  try {
-    return await repository.getFileLatestModifiedDateAsync(relativePath);
-  } catch {
-    logger.warn(
-      "Failed to get the last modified timestamp from Git for the file",
-      relativePath
-    );
-  }
+  // 禁用 Git 最后修改时间戳获取，避免构建环境无法访问 Git 历史时产生警告
+  return;
 }
 export {
   loader
